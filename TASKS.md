# المهامّ

> ما اتُّفق على عمله وترتيبُه وسببُه — يُحدَّث مع كلّ بندٍ يُنجَز.
> آخر تحديث: ٢٣ سبتمبر ٢٠٢٦

## القرار الذي يحكم هذا الملفّ

**النسخة القائمة على `mazad.nx.sa` هي الإنتاج**، وتدخلها بياناتٌ حقيقية خلال
أيّام. لا بيئة staging الآن، ولا انتقال إلى AWS أو جوجل.

وهذا يقلب ترتيب الأولويّات: ما كان «يُؤجَّل بلا ضرر» وهو يحمل حساباتِ تجربة،
يصير قبل دخول المال **شرطًا**. فالبنود مرتّبةٌ بهذا الحدّ لا بصعوبتها.

---

# حالة التطبيق — ٢٢ سبتمبر ٢٠٢٦

## ✅ تمّ ويعمل على `mazad.nx.sa`

| | الدليل |
|---|---|
| قاعدة بيانات حقيقية | البيانات تبقى بعد النشر — جُرّب |
| أداة نسخٍ واستعادة | [`db-backup.sh`](scripts/db-backup.sh) · [`db-restore.sh`](scripts/db-restore.sh) |
| ستّ ترويسات أمان + `CSP` بـ`nonce` | قِيست على الحيّ، والـnonce يتغيّر بين طلبين |
| حدُّ معدّلٍ بالعنوان | يشتدّ بكلاودفلير بلا تعديل كود |
| قفل المسح | مسحان متوازيان لا يجتمعان |
| بوّابة سلسلة التوريد | `pnpm audit --prod` نظيف · `gitleaks` على ٦٧ إيداعًا |
| محاولة اختراق | **١١/١١** — السعر الاحتياطيّ وأرقام الجوال والصلاحيات |
| صفحة الخصوصية | `/privacy` = 200 |

## ✅ تطبيق iOS — جاهزٌ للرفع

| | |
|---|---|
| `.ipa` موقَّع | يُبنى في السحابة بلا Xcode · ٨٤٥ ك.ب |
| الاستحقاقات | `aps-environment` · `time-sensitive` · `associated-domains` — **مقروءةً من داخل الحزمة** |
| بيان الخصوصية | `PrivacyInfo.xcprivacy` في الحزمة |
| الأيقونات | من هويّة المنصّة لا من Capacitor |
| قناة APNs | مباشرةً بلا Firebase — ٨ فحوص |
| أصول المتجر | لقطاتٌ ١٢٩٠×٢٧٩٦ · رسمٌ دعائيّ · نصوصٌ بحدودها |

## ⏳ ما بقي

| # | ما هو | من |
|---|---|---|
| ١ | **تحقّق أنّ APNs يعمل** — لوحة الإدارة ← الجوّال | أنت |
| ٢ | **وسمٌ يرفع إلى TestFlight** | أنت — أمرٌ واحد |
| ٣ | تعبئة صفحة المتجر من `store-assets/` | أنت |
| ٤ | **أندرويد**: حساب Play · مفتاح التوقيع · Firebase | أنت وأنا |
| ٥ | ملفّ مبادلة على السيرفر — تحسينٌ لا علاجٌ لعطبٍ قائم | أنت — أوامرُه في [النشر §٩٫٦](docs/deployment.md) |
| ٦ | تفعيل النسخ الاحتياطيّ المجدوَل | أنت |

## 📌 مؤجَّلٌ بقرار

`staging` · النسخ المتعدّدة · السحابة · رمز CSRF · RLS · تحقّق الإدارة بخطوتين ·
كلاودفلير (الكود جاهزٌ له) · مفتاح طريقة التحصيل

## 🔭 المشروع الثاني — صنفٌ آخر

قِيس في ٢٢ سبتمبر: **٣٨ ملفًّا من ٢٩٠** تعرف «اللوحة» بحقولها (١٣٪).
والمحرّك الماليّ **صفرٌ**: `auction.ts` و`wallet.ts` و`money.ts` و`zatca.ts`
و`order-timeline.ts` و`reference.ts` و`countdown.ts` — لا إشارة واحدة.

فالمزاد والعربون والضمان والعمولة والضريبة والفواتير تُنقل كما هي. ويُبدَّل:
رسمُ الصنف (١٢١٥ سطرًا)، وحقولُه في `types.ts` و`schemas.ts`، وفلاترُه.

> **ولا يُجرَّد قبل معرفة الصنف الثاني بعينه.** تجريدٌ من مثالٍ واحد يُنتج
> واجهةً تظنّها عامّة فإذا الثاني يحتاج ما لم تتوقّعه. التجريد من مثالين.

---

# ثغراتٌ مقيسة — لا مفترضة

> هذه ليست مخاوف عامّة عن الأمن، بل ما ظهر بقراءة كود المنصّة نفسها في
> ٢٠ سبتمبر ٢٠٢٦. وهي **رأس المهامّ**: البنود أدناه تفصيلُ علاجها.

## ~~ثغرة ١~~ ✅ — حدُّ المعدّل كان بالبريد لا بالشبكة

```ts
// src/app/api/auth/login/route.ts
rateLimit(`login:${body.email}`, 8, 60_000)
```

المفتاح هو **البريد**، لا عنوان الشبكة. فمن يهاجم بألف بريدٍ مختلف لا يصطدم
بحدٍّ أصلًا — وكلُّ عنوانٍ في القائمة يأخذ ثماني محاولاتٍ كاملةً في الدقيقة.
والحدُّ قائمٌ في التسجيل ودخول الإدارة وشحن المحفظة بالطريقة نفسها.

**سُدّت في ٢٠ سبتمبر ٢٠٢٦ — في الكود لا بانتظار كلاودفلير**: أُضيف حدٌّ
**بالعنوان** مع الحدّ بالبريد في الدخول والتسجيل ودخول الإدارة، عبر
[`client-ip.ts`](src/lib/server/client-ip.ts) الذي يقرأ `cf-connecting-ip`
ثمّ `x-forwarded-for`. فالحماية قائمةٌ اليوم خلف Traefik، **وتشتدّ** يوم
يُوضع كلاودفلير بلا تعديل سطرٍ واحد — [البند ٣](#٣-كلاودفلير-أمام-المنصّة-وهو-الـcdn-نفسه).

## ~~ثغرة ٢~~ ✅ — كانت المنصّة بلا ترويسة أمانٍ واحدة

`next.config.mjs` ليس فيه `headers()`، ولا يوجد `middleware` في المشروع
إطلاقًا. فلا `HSTS` ولا `CSP` ولا `X-Frame-Options` ولا `nosniff` ولا
`Referrer-Policy`.

**نُفِّذت في ٢٠ سبتمبر ٢٠٢٦**: سبعٌ ساكنة في `next.config.mjs`، و`CSP`
بـ`nonce` لكلّ طلب في `src/middleware.ts` مع `strict-dynamic`. وأُثبتت على
المجموعة كاملةً — **١٣٧/١٣٧** — وخمسةُ فحوصٍ جديدة تقرأ وحدة تحكّم المتصفّح
فتلتقط أيّ خرق. — [البند ٤](#٤-ترويسات-الأمان-تمّ).

## ~~ثغرة ٣~~ ✅ — كانت البوّابة لا تفحص ما لا يُقاس بالقراءة

`ci.yml` يفحص الأنواع والتدقيق والاختبارات ويبني الصورة — ولا يفحص:

| ما ينقص | ما يمرّ بسببه |
|---|---|
| `pnpm audit` | ثغرةٌ معروفة في حزمةٍ من حزمنا، تُنشر بلا أن يراها أحد |
| `gitleaks` | مفتاحٌ يُدفع سهوًا إلى المستودع، ويبقى في التاريخ بعد حذفه |

**نُفِّذا في ٢٠ سبتمبر ٢٠٢٦** في وظيفة `supply-chain` مستقلّة. وأصلحا شيئًا
فعليًّا: `postcss@8.4.31` كان يصل الإنتاج عبر `next` بثلاث ثغرات إحداها عالية،
و`uuid@7` عبر أدوات Capacitor — رُفعا بـ`pnpm.overrides`، **فصارت تبعيات
الإنتاج نظيفةً تمامًا**. و`gitleaks` مسح ٦٧ إيداعًا فلم يجد إلّا مفتاح المثال
في معيار WebSocket. — [البند ٧](#٧-مراجعةٌ-أمنية-شاملة-بدأت).

## وفخّان في كلاودفلير — يُفسدان ما جاء لأجله

**١) `SSL/TLS` على `Full (strict)` لا `Flexible`.** «Flexible» تشفّر ما بين
الزائر وكلاودفلير، وتترك الوصلة بين كلاودفلير **وسيرفرك بلا تشفير**. وهي أسوأ
من لا شيء: المتصفّح يُظهر قفلًا، والقفل كاذب.

**٢) تحقّق أنّ صفحات HTML لا تُخزَّن.** المطلوب `cf-cache-status: DYNAMIC` على
صفحات HTML، و`HIT` على `/_next/static/*` وحدها. وصفحةٌ مخزَّنة وفيها جلسة
مستخدمٍ تُعرض لزائرٍ آخر هي **تسريبُ حساب** — لا بطء ولا خطأ يُنبّه إليه.

---

# أ — قبل أن يدخل مالٌ حقيقيّ

## ١ و٢. النسخ الاحتياطيّ والاستعادة — **الأداة جاهزة**

بُنيت في ٢٠ سبتمبر ٢٠٢٦ بعد قرار «ابدأ بالنسخ»:
[`db-backup.sh`](scripts/db-backup.sh) يأخذ النسخة **ويتحقّق أنّها تُقرأ**،
و[`db-restore.sh`](scripts/db-restore.sh) يستعيدها في قاعدةٍ جانبية ويطابق
أعدادها بالحيّة. الدليل في [التوثيق](docs/backup-and-restore.md).

| ما بقي | من ينفّذ |
|---|---|
| **تشغيلها مرّةً على السيرفر**، ونقلُ الملفّ إلى جهازك | **أنت** — سطران، [§٥](docs/backup-and-restore.md) |
| جدولةٌ يوميّة في كوليفاي مع رفعٍ خارجيّ | **أنت** — تبويب Backups في خدمة القاعدة |
| النسخ المُدار والعودة إلى لحظة (PITR) | يُفعَّل **عند الانتقال السحابيّ** — [القسم ج](#ج--مؤجَّلٌ-بقرار) |

> والأداة تنتقل مع المشروع — ملفّاتٌ في المستودع لا تعتمد إلّا على
> `DATABASE_URL` و`pg_dump`. **والمُجدوِل وحده** هو ما يُعاد ضبطه في المنصّة
> الجديدة.

## ٣. كلاودفلير أمام المنصّة (وهو الـCDN نفسه)

**قيمته ليست السرعة وحدها.** قِسنا الكود: حدُّ المعدّل مبنيٌّ على **البريد
ومعرّف المستخدم** لا على عنوان الشبكة —

```
rateLimit(`login:${body.email}`, 8, 60_000)
```

فمن يهاجم بثمانية آلاف بريدٍ مختلف **لا يصطدم بحدٍّ أصلًا**. وهذه ثغرةٌ لا
يسدّها إلّا حدٌّ فوق طبقة الشبكة — وهو ما يعطيه كلاودفلير بلا سطرِ كود.

**ما يُضبط**:

- [ ] DNS عبر كلاودفلير، والسحابة **برتقالية** (proxied)
- [ ] `SSL/TLS` على **Full (strict)** — و«Flexible» تجعل الوصلة بين كلاودفلير
      وسيرفرك بلا تشفير، وهي أسوأ من لا شيء لأنّها تُظهر قفلًا كاذبًا
- [ ] `Always Use HTTPS`
- [ ] قواعد WAF المُدارة
- [ ] **حدّ معدّل على `/api/auth/*` و`/api/admin/*`** — وهو المقصد الأوّل
- [ ] `Bot Fight Mode`

**وفخّان يجب التحقّق منهما بعد التفعيل**:

1. **`/ws` لا ينكسر.** كلاودفلير يدعم WebSocket، لكن لا تُصدّق — افتح صفحة
   مزادٍ من جهازين وزايد، فإن لم يتحرّك السعر عند الثاني فالترقية محجوبة.
2. **لا تُخزَّن صفحةٌ فيها جلسة.** تحقّق أنّ `cf-cache-status` على صفحات
   HTML = `DYNAMIC`، وعلى `/_next/static/*` = `HIT`. وصفحةٌ مخزَّنة لمستخدمٍ
   تُعرض لآخر تسريبُ حساب.

| | |
|---|---|
| **الكود** | ✅ **مهيّأ** — `client-ip.ts` يقرأ `cf-connecting-ip` أوّلًا. لا تعديل يلزم يوم تُفعّله |
| من ينفّذ | **أنت** — لوحة كلاودفلير وحدها، وأنا أتحقّق بعدها بالقياس |
| الكلفة | ساعة |
| الدليل | مزايدةٌ تصل بين جهازين · `cf-cache-status` صحيح على الاثنين |

## ٤. ترويسات الأمان (تمّ)

**نُفِّذ في ٢٠ سبتمبر ٢٠٢٦.** سبعٌ ساكنة في [`next.config.mjs`](next.config.mjs)،
و`CSP` بـ`nonce` في [`src/middleware.ts`](src/middleware.ts).
التفصيل في [الأمان §١٢٫٥](docs/security.md).

| الترويسة | ما تمنع |
|---|---|
| `Strict-Transport-Security` | هبوطًا إلى HTTP يُمكّن من الاعتراض |
| `X-Frame-Options` / `frame-ancestors` | وضعَ المنصّة في إطارٍ لسرقة النقرات |
| `X-Content-Type-Options: nosniff` | تخمينَ المتصفّح لنوع ملفٍّ مرفوع |
| `Referrer-Policy` | تسريبَ مسارٍ داخليّ إلى موقعٍ خارجيّ |
| `Permissions-Policy` | طلبَ كاميرا أو موقعٍ من كودٍ دخيل |
| `Content-Security-Policy` | **تنفيذَ سكربتٍ حُقن في الصفحة** — الأقوى والأصعب |

> الخمسة الأولى سطورٌ في `next.config.mjs`. أمّا `CSP` فيحتاج `middleware`
> يولّد `nonce` لكلّ طلب، لأنّ Next يضع سكربتاتٍ سطرية. وهي **أهمّها** ولا
> تُختصر: بلا CSP، ثغرةُ XSS واحدة تُساوي حساب أدمن.

| | |
|---|---|
| الحالة | ✅ مدموج — البوّابة **١٣٧/١٣٧** و**٤٢٥** وحدة |
| الحارس | [`security-headers.spec.ts`](tests/e2e/security-headers.spec.ts) — يقيس كلّ ترويسة، ويتحقّق أنّ الـnonce يتغيّر بين طلبين، ويقرأ وحدة تحكّم المتصفّح على صفحاتٍ عامّة وخاصّة |
| مخرجٌ بلا نشرة | `CSP_REPORT_ONLY=1` يحوّلها إلى وضع التقرير — متغيّرٌ في لوحة النشر، لا بناءٌ جديد |
| ما بقي | `preload` في HSTS — تُؤخذ بعد استقرار النطاق وفروعه، فهي لا تُتراجع بسهولة |

## ٥. قائمة الفحص قبل الإنتاج (قِيست على الحيّ)

موجودة في [`docs/security.md §14`](docs/security.md) و[`docs/deployment.md §10`](docs/deployment.md)،
ولم تُنفَّذ على النشرة الحيّة بندًا بندًا:

قِيست على `mazad.nx.sa` في ٢٠ سبتمبر ٢٠٢٦:

| البند | النتيجة |
|---|---|
| `SESSION_SECRET` خاصٌّ بالبيئة | ✅ — جُرّب المفتاح الافتراضيّ على `/api/internal/sweep` فرُدَّ بـ403 |
| `/api/internal/sweep` محروس | ✅ 403 بلا مفتاح |
| `DEMO_MODE=false` | ✅ لا تلميحات ديمو في صفحة الدخول |
| `NEXT_PUBLIC_APP_URL` | ✅ `https://mazad.nx.sa` مخبوزٌ في الحزمة |
| كوكي الجلسة | ✅ `httpOnly` · `sameSite=lax` · `secure` في الإنتاج |
| حدود المعدّل | ✅ بالبريد **وبالعنوان** — البند ٤ |
| الترويسات السبع و`CSP` | ⏳ **مدموجةٌ وتنتظر نشرتك** |
| كلمة الأدمن بُدِّلت | ⏳ **عندك أنت** — لا أقيسه ولا أطلبه |

| | |
|---|---|
| الحالة | ✅ ستّةٌ مقيسةٌ خضراء · بندان ينتظران نشرتك وقرارك |

---

# ب — بعد دخول البيانات مباشرةً

## ٦. قفلٌ على المسح الدوريّ (تمّ)

**الثغرة**: `finalizeDueAuctions` تقرأ ثمّ تكتب بلا قفل، وحارسُها
`existing.length === 0` فحصٌ ثمّ فعل. ونسختان من التطبيق تمرّان به معًا
فتُنشئان **صفقتين لمزادٍ واحد**. ولا تمسكها فرادةُ `orders` لأنّها على
`(buyerId, clientRequestId)` والمسح لا يمرّر `clientRequestId`.

**ولماذا الآن وأنت على نسخةٍ واحدة؟** لأنّ ما يحميك **عُرفٌ في التوثيق لا
حارسٌ في الكود**. يكفي أن تُضبط نسختان في كوليفاي يومًا، فتظهر صفقاتٌ مكرّرة
بلا رسالة خطأ واحدة — وفيها مال.

**نُفِّذ في ٢٠ سبتمبر ٢٠٢٦**: `pg_try_advisory_lock` في
[`withAdvisoryLock`](src/lib/store/pg/client.ts) — من ظفر به مسح، والبقية
تتخطّى بلا انتظار ولا خطأ.

| | |
|---|---|
| الحالة | ✅ مدموج |
| الحارس | [`sweep-lock.test.ts`](tests/unit/sweep-lock.test.ts) — مسحان متوازيان لا يجتمعان قطّ، والقفل يُفكّ ولو سقط العمل، ولا يُستنزف المَجمع. **جُرّب بإزالة القفل فسقط** |

## ٧. مراجعةٌ أمنية شاملة (بدأت)

ما وجدتُه بالقياس حتى الآن، مرتّبًا بالخطر:

| # | الثغرة | الحال | العلاج |
|---|---|---|---|
| ~~١~~ | ~~لا ترويسات أمان~~ | ✅ **تمّ** | البند ٤ |
| ~~٢~~ | ~~حدّ المعدّل بالبريد لا بالشبكة~~ | ✅ **تمّ** — حدٌّ بالعنوان في الكود، ويشتدّ بكلاودفلير | البند ٣ يبقى طبقةً فوقه |
| ٣ | **لا رمز CSRF صريح** | يُعتمَد على `SameSite=Lax` وحده | رمزٌ مزدوج على الطلبات المغيِّرة |
| ~~٤~~ | ~~لا فحص ثغرات~~ | ✅ **تمّ** — `pnpm audit --prod` يُسقط البناء، وتبعيات الإنتاج صارت نظيفة | وظيفة `supply-chain` |
| ~~٥~~ | ~~لا فحص أسرار~~ | ✅ **تمّ** — `gitleaks` على **التاريخ كلّه**، ٦٧ إيداعًا نظيفة | وظيفة `supply-chain` |
| ٦ | **لا RLS في القاعدة** | طبقة حمايةٍ ثانية غائبة، والخدمة وحدها الحارس | سياسات RLS |
| ٧ | **لوحة الإدارة بعاملٍ واحد** | بريدٌ وكلمة فقط | تحقّقٌ بخطوتين، أو حصرٌ بعناوين معروفة |

**والثلاثة التي تُفحص ولا تُفترض — ✅ نُفِّذت في ٢٠ سبتمبر ٢٠٢٦**، ونجحت
**١١/١١** في [`attack.spec.ts`](tests/e2e/attack.spec.ts):

| الوعد | النتيجة |
|---|---|
| السعر الاحتياطيّ لا يبلغ مشاهدًا | ✅ زُرع `987,654` ثمّ طُلب غريبًا ومسجَّلًا، صفحةً وAPI — لم يتسرّب |
| وسعر الابتداء العلنيّ **يظهر** | ✅ فحصٌ عكسيّ — وإلّا كان الفحص يقيس صفحةً فارغة |
| أرقام الجوال | ✅ خمس صيغ، في السوق والإعلانات والمعارض وAPI |
| رقم العضوية | ✅ لا يظهر في صفحةٍ علنية |
| إعلانُ غيره: تعديلًا وإلغاءً وحذفًا | ✅ رُدّ، والإعلان لم يُمسّ |
| مسارات الإدارة قراءةً وكتابةً | ✅ رُدّت — **وبالأساليب الصحيحة** لا بـ405 كاذب |
| المسار الداخليّ بجلسةِ مستخدم | ✅ 403 |

| | |
|---|---|
| تمّ | ١ (الترويسات) · ٢ (حدّ العنوان) · ٤ (التبعيات) · ٥ (الأسرار) |
| بقي | ٣ رمز CSRF · ٦ سياسات RLS · ٧ تحقّق الإدارة بخطوتين — **ولا شيء منها يمنع إطلاق تطبيق** |
| الدليل | فحوصٌ في المجموعة تسقط عند كلّ ثغرةٍ منها، لا تقريرٌ يُقرأ مرّة |

---

# د — مجدوَلٌ بعد الأولويّات

## ٨. الروابط العميقة — **نصفُها قائمٌ اليوم**

سُجّل في ٢٣ سبتمبر ٢٠٢٦، **وأُخّر إلى آخر البنود بقرار صاحب المنصّة في ٢٤
سبتمبر**: «اتركها آخر شيء».

ولا يُبنى من الصفر — هذا ما قِيس في المستودع اليوم:

| القطعة | الحال |
|---|---|
| `associated-domains` في الاستحقاقات | ✅ `applinks:mazad.nx.sa` — [`App.entitlements`](ios/App/App/App.entitlements) |
| ملفّ آبل `apple-app-site-association` | ✅ يُقدَّم بصيغة `components` الحديثة، ويُردّ **404** بلا `APPLE_TEAM_ID` فلا يُوهم آبل بارتباطٍ لا يصحّ |
| ملفّ جوجل `assetlinks.json` | ✅ يُقدَّم، ويُردّ **404** بلا `ANDROID_CERT_FINGERPRINTS` |
| مستقبِل `appUrlOpen` في الغلاف | ✅ في [`native-shell.tsx`](src/components/layout/native-shell.tsx) — يأخذ **المسار وحده** ويرفض ما جاء من نطاقٍ آخر |

### وما ينقص — وهو ما يجعلها لا تعمل اليوم

| # | الناقص | لماذا يمنع |
|---|---|---|
| ١ | **`intent-filter` لروابط الويب في أندرويد** | [`AndroidManifest.xml`](android/app/src/main/AndroidManifest.xml) فيه `MAIN/LAUNCHER` وحده. فبلا `VIEW` + `BROWSABLE` + `android:autoVerify="true"` على `https://mazad.nx.sa` **لا يصل الرابط إلى التطبيق أصلًا** — ولا ينفع `assetlinks.json` وحده |
| ٢ | `APPLE_TEAM_ID` و`ANDROID_CERT_FINGERPRINTS` في بيئة الإنتاج | الملفّان يُردّان 404 بدونهما بالتصميم — فالارتباط لا ينعقد |
| ٣ | مطابقةُ المسارات بين الملفّين والشاشات | ملفّ آبل يفتح `/market/*` و`/account/*` و`/u/*` و`/@*`. وأندرويد سيحتاج القائمة نفسها حرفًا بحرف، وإلّا اختلف سلوك النظامين |
| ٤ | **زرّ مشاركةٍ يولّد الرابط** | لا قيمة لرابطٍ يفتح التطبيق إن لم يكن ثمّة ما يُنتجه. و`@capacitor/share` مُثبَّتٌ سلفًا في التبعيات |
| ٥ | حارسٌ في المجموعة | يُقاس أنّ الملفّين يُقدَّمان بنوعهما الصحيح بلا إعادة توجيه، وأنّ المسارات المستثناة (`/admin/*` و`/api/*`) **لا** تُفتح في الغلاف |

### الفخّ الذي يُسقط أكثر التنفيذات

**الملفّان لا يُجلبان إلّا عند التثبيت أو التحديث.** فتصحيحُ مسارٍ فيهما بعد
نشر التطبيق لا يصل إلى جهازٍ مثبَّتٍ سلفًا إلّا بإعادة تثبيت — ولذلك تُضبط
القائمة **قبل** أوّل رفعٍ إلى المتجرين لا بعده.

وثانٍ: ملفّ آبل **لا يُقبل خلف إعادة توجيه** ولو إلى النطاق نفسه بـ`www`.
فإن وُضع كلاودفلير أمام المنصّة ([البند ٣](#٣-كلاودفلير-أمام-المنصّة-وهو-الـcdn-نفسه))
يُتحقّق أنّ `/.well-known/apple-app-site-association` يُردّ **200 مباشرةً**
بنوع `application/json`، لا 301 ثمّ 200.

| | |
|---|---|
| المدّة المقدَّرة | نصف يوم — أكثرُه ضبطٌ وتحقّق لا كتابة |
| يبدأ | **آخرًا** — بعد كلّ ما في هذا الملفّ |

---

## ٩. الستوريز والبنرات وتخزين R2 — ✅ تمّ في ٢٤ سبتمبر ٢٠٢٦

| | |
|---|---|
| الرئيسية | ستوريز تُفتح ملءَ الشاشة، وبنراتٌ ٢:١ بمدّةٍ تنتهي بنفسها — على الجوال والتطبيق وحدهما |
| التحرير | [`/admin/home`](src/app/admin/%28panel%29/home/page.tsx) — قسمان بشاراتٍ تقول لماذا لا يُرى ما نُشر |
| التخزين | R2 ببادئتين: `platform/**` عامّ، و`users-files/<id>/**` لصاحبه والإدارة وحدهما |
| التوقيع | SigV4 مكتوبٌ لا مستورَد — يُقاس بمتّجهات أمازون المنشورة حرفًا بحرف |
| الفحوص | **٥٥٢ وحدة · ١٩١ متصفّح** — كلُّها خضراء |
| التوثيق | [تخزين الوسائط](docs/media-storage.md) · [واجهة الرئيسية](docs/home-surface.md) |

### النشرُ الآن — ماذا يظهر وماذا لا

**تنشر اليوم بلا R2 وكلُّ شيءٍ يعمل**: الترحيل يجري عند الإقلاع تلقائيًّا،
والجدولان يُنشآن، ولوحة `/admin/home` تُحرَّر، والستوريز والبنرات تظهر على
الجوال. والملفّات تسكن **الحجم الدائم** `/app/data/media` فتبقى بعد النشر.

| | بلا R2 | بـR2 |
|---|---|---|
| الرفع والعرض | ✅ يعملان | ✅ |
| بقاء الملفّات بعد النشر | ✅ ما دام `/app/data` مربوطًا بحجمٍ دائم | ✅ مستقلٌّ عن الخادم |
| الفدّيو | ⚠️ يمرّ بالخادم — وصلةٌ طويلة لكلّ مشاهد | ✅ من كلاودفلير |
| لو تعدّدت النسخ | ❌ كلُّ نسخةٍ بملفّاتها | ✅ مشتركة |

> ⚠️ **وشرطٌ واحد لا يُترك**: `Coolify → Storages → Volume Mount: /app/data`.
> وبلاه تُمحى الملفّات مع كلّ نشرة **وتبقى صفوفُها في القاعدة** — فتُعرض
> صورةٌ مكسورة بلا رسالةِ خطأ واحدة. وهو المجلَّد نفسه الذي تحتاجه الإعدادات.

### ✅ R2 يعمل على الحاوية الحقيقية — ٢٤ سبتمبر ٢٠٢٦

الرفعُ من `/admin/home` يسكن `mazad-media` فعلًا. وقد سبقه عطبٌ أسقطه كلَّه:

**`x-amz-content-sha256` لم تكن تُرسل ولا تُوقَّع.** تعليقٌ في `sigv4.ts` كان
يقول «يضيفها `r2.ts`»، و`r2.ts` لا يضيفها — عقدٌ كُتب ولم يُنفَّذ. وS3 تشترطها
موقَّعة، فردّ R2 بـ`403` على **كلّ** طلبٍ موقَّع. وأخطرُ أثرِه أنّ `read` تردّ
`null` لكلّ ردٍّ غيرِ ناجح، **فالرفضُ كان يُقرأ «ملفًّا غير موجود»** — فيبدو
القياسُ سليمًا وليس فيه شيءٌ سليم.

وقِيس على حاويةِ المنصّة لا على محاكاة: بتوقيع التطبيق `403`، وبما تشترطه S3
`200`. ثمّ قُوبل توقيعُ المكتبة بعد الإصلاح بما قبِله R2 فتطابقا حرفًا.

> **والدرسُ أعمُّ من الترويسة**: توقيعٌ صحيحٌ حسابًا يُردّ لأنّ الخدمةَ تشترط
> ما لا تشترطه الصيغة. فلا يُصدَّق محرّكُ تخزينٍ حتى يُجرَّب على حاويةٍ
> حقيقية — ولو مرّةً بخمسة بايتات. وفحوصُ التوقيع كانت تقيس الحسابَ وهو صحيح،
> لا **العقدَ** مع الخدمة. فأُضيف ما يقيس العقد.

### ⚠️ ١٢. مسارُ الملفّ الخاصّ لم يُجرَّب على R2 بعد

`users-files/<id>/**` يُقدَّم برابطٍ موقَّعٍ مسبقًا من `presignUrl` — وهي
**الدالّة الوحيدة في المحرّك التي لم تُجرَّب على حاويةٍ حقيقية**. والعطبُ الذي
انكشف اليوم من صنفٍ لا تمسكه فحوصُ التوقيع، فلا تُفترض سلامتُها بالقياس على
أختها.

والفرقُ في الصيغة حقيقيّ: الموقَّعُ مسبقًا يحمل كلَّ شيءٍ في **سلسلة
الاستعلام** ويوقّع `host` وحده، فلا تلزمه `x-amz-content-sha256`. فالأرجحُ
سلامتُه — **والأرجحُ ليس مقيسًا**.

ويُقاس بطلبِ مفتاحٍ لا وجود له من الحاوية الخاصّة برابطٍ موقَّع: `404` توقيعٌ
سليم، و`403` توقيعٌ مرفوض.

### ⏳ ما ينتظرك أنت

| # | ما هو | لماذا |
|---|---|---|
| ٠ | **ربطُ `/app/data` بحجمٍ دائم في كوليفاي** — إن لم يكن مربوطًا | شرطٌ للإعدادات (وللوسائط إن عاد التخزينُ إلى القرص) |
| ٠٫٥ | 🔴 **تبديلُ رمز R2** — مفتاحاه ظهرا في محادثة | سرٌّ ظهر يُبدَّل: أنشئ رمزًا بنفس الصلاحيات ← بدّله في كوليفاي ← Restart ← **ثمّ احذف القديم**، فإنشاءُ جديدٍ لا يُبطل القديم |
| ~~١~~ | ~~حاوية R2 ورمزٌ بصلاحية Object Read & Write~~ | ✅ تمّ، ومقيسٌ على الحاوية |
| ٢ | **ربط `cdn.mazad.nx.sa` بالحاوية** وضبط `R2_PUBLIC_BASE_URL` | بدونه يمرّ **كلُّ بايت فدّيو بالخادم**، ووصلةٌ طويلة لكلّ مشاهد تزاحم ما تحتاجه المزايدة اللحظية |
| ٣ | **ولا تُجعل الحاوية عامّةً بالكامل** | النطاق المخصّص يقدّم ما تحت `platform/` وهو المقصود. وفتحُ الحاوية كلِّها يُبطل الفصل، فتصير وثائقُ المستخدمين مفتوحةً لمن بلغ رابطَها |

والخطوات بتفصيلها في [تخزين الوسائط §٦](docs/media-storage.md).

---

## ١٠. تذبذبُ البوّابة — سقطتان تتبدّلان في كلّ تشغيلة

سُجّل في ٢٤ سبتمبر ٢٠٢٦. **المجموعة تمرّ محلّيًّا ١٩١/١٩١ مطّردًا**، وتسقط
في البوّابة سقطةً أو سقطتين **تتبدّلان** — وهذا بعينه ما يُفرّق بين عطبٍ
في المنتج وضعفٍ في البيئة.

| الفحص | e6ae464 | 2f67408 | f4b6790 |
|---|---|---|---|
| `plate-formats` — الحرف يتجاوز خانته | ✘ | ✘ | ✅ **أُصلح** |
| `checkout:88` — وسيلة معطّلة | ✓ | ✘ | ✘ |
| `native-shell:422` — مفتاح الإعدادات | ✓ | ✘ | ✓ |
| `design:296` — التواريخ | ✘ | ✓ | ✓ |
| `design:531` — كشف الحساب | ✓ | ✓ | ✘ |

والعَرَض واحدٌ موثَّقٌ في [`playwright.config.ts`](playwright.config.ts):
الصفحة تُقاس **فارغة** (`main` بلا نصّ) أو **مكرّرة** (شجرتا تصيير). وسقطةُ
`checkout` تقولها صراحةً: انتقلت إلى `/account/purchases` ثمّ انتظرت
`/checkout/` ستّين ثانية.

### والسبب مُرجَّحٌ لا مقطوعٌ به

**خادمٌ واحد يخدم مئةً وواحدًا وتسعين سياقًا بالتتابع** (`workers: 1`)، وكلُّ
صفحةٍ `force-dynamic` — فلا شيء يُخدَم من ذاكرة. ويزيده أنّ الفحوص تتشارك
**منصّةً واحدة في الذاكرة**: ما يزرعه فحصٌ يبقى لمن بعده، و«أكمل السداد»
الأولى قد تكون صفقةً أغلقها غيرُه.

> وقد وقع هذا فعلًا في `home-media.spec.ts` فعولج بـ«ما زُرع يُقلَع» —
> والعلاج نفسه لم يُعمَّم على بقيّة الملفّات.

### ما يُجرَّب، بالترتيب

| # | ما هو | لماذا |
|---|---|---|
| ١ | **قلعُ ما يُزرع في كلّ ملفّ** — كما في `home-media.spec.ts` | أرخصُها، ويُزيل التلوّث وهو السبب الأرجح لسقطة `checkout` |
| ٢ | بذرةٌ معزولة لكلّ ملفّ (`storageState` + بادئةُ بيانات) | يقطع التشارك من أصله |
| ٣ | تقسيم المجموعة على عدّة وظائف (`--shard`) | يُخفّف الازدحام، ويُقصّر الزمن من ٦٫٥ دقائق |
| ٤ | `retries: 1` في البوّابة وحدها | **آخرُ الحلول**: يُخفي العَرَض ولا يُعالجه، ويُقبل بعد الثلاثة لا قبلها |

| | |
|---|---|
| الخطر | **لا يمنع نشرًا**: العطب في قياس البيئة لا في المنتج |
| الكلفة | نصف يومٍ للأوّل والثالث |
| يبدأ | بعد ربط R2 وقبل الروابط العميقة |

---

## ١١. نشرةٌ تسقط عند `COPY` — والسبب لم يُعرف بعد

سقطت نشرةُ ٢٣ سبتمبر عند:

```
#16 [build 4/6] COPY --from=deps /app/node_modules ./node_modules
… ثمّ انقطاعٌ بلا سطر خطأ، و`exit code 255`
```

### ما استُبعد بالقياس — لا بالظنّ

قِيس على الخادم في ٢٤ سبتمبر، **وكلُّه نفى الفرضية الأولى**:

| الفرضية | القياس | الحكم |
|---|---|---|
| ذاكرةٌ تنفد | `Mem: 15Gi` و**`available 11Gi`** | ❌ مستبعَدة |
| قرصٌ يمتلئ | `193G` و**`167G` حرّة** (١٤٪) | ❌ مستبعَدة |
| النظام قتل البناء | `dmesg \| grep "killed process"` **فارغ** | ❌ مستبعَدة |

> وكُتب في [النشر §٩٫٦](docs/deployment.md) أنّ ملفّ المبادلة علاجُها — وهو
> صحيحٌ **للصورة الأولى** من العطب (سقوطٌ في `next build` بـ`ELIFECYCLE`)،
> ولا ينطبق على هذه: الذاكرة واسعة، والسقوط قبل البناء.

### ما بقي مرشَّحًا

| # | المرشَّح | ما يُجرَّب |
|---|---|---|
| ١ | **ذاكرةُ بناءٍ تالفة** — `Build Cache` ٣٫٨ غيغا كلُّها قابلةٌ للاسترجاع و**لا شيء منها نشِط** | `docker builder prune -af` ثمّ إعادة النشر |
| ٢ | حدٌّ على حاوية البناء في كوليفاي نفسه | يُقرأ من إعدادات الخادم في كوليفاي |
| ٣ | خللٌ في BuildKit — والنظام يقول **`*** System restart required ***`** و**ثلاث عمليات زومبي** | إعادةُ إقلاع الخادم بعد تفريغ الذاكرة |

### وأوّلُ ما يلزم: **السجلّ كاملًا**

السجلُّ المتاح **مقطوعٌ عند موضع السقوط** ولم يُلتقط فيه سطرُ الخطأ نفسه.
وكوليفاي يقول: «**Show Debug Logs**». فما لم يُقرأ ذلك السطر يبقى التشخيص
ظنًّا — وقد أخطأ الظنُّ مرّة.

| | |
|---|---|
| الخطر | **يمنع كلّ نشرة** حتى يُعرف |
| يبدأ | فورًا |

---

# ج — مؤجَّلٌ بقرار

## النسخ الاحتياطيّ والاستعادة (البندان ١ و٢)

**القرار، وقد تبدّل في اليوم نفسه**: أُجّل النسخ أوّلًا إلى ما بعد الانتقال
السحابيّ، ثمّ قال صاحب المنصّة «ابدأ بالنسخ» حين تبيّن أنّه لا يستلزم سحابة.
**فبُنيت الأداة وأُثبتت** ([البندان ١ و٢](#١-و٢-النسخ-الاحتياطيّ-والاستعادة--الأداة-جاهزة)).

والمؤجَّل الآن هو **النسخ المُدار وحده**: العودة بالقاعدة إلى ما قبل عشر
دقائق (PITR) — وهي لا تأتي إلّا من خدمةٍ سحابية، وتأجيلها معقول.

**الخطر الباقي**: أقصى ما يُفقد هو ما بين آخر نسخةٍ والعطب — يومٌ إن كانت
النسخة يومية. وكان قبل اليوم **كلَّ شيء**.

**ما يُنفَّذ عند الانتقال**: تفعيل النسخ المُدار، ثمّ **استعادةٌ مُجرَّبة
واحدة** قبل اعتماده. نسخةٌ لم تُستعَد قطّ ليست نسخة، بل ظنٌّ بها — والأعطال
المعتادة (نسخةٌ فارغة، صلاحيات ناقصة، إصدار بوستجرس مختلف) لا تظهر إلّا عند
أوّل استعادة، وأسوأ وقتٍ لاكتشافها يوم الكارثة.

## بقيّة المؤجَّل

| البند | لماذا أُجّل | متى يُعاد النظر |
|---|---|---|
| **بيئة staging** | مستخدموك حساباتُ تجربة، وبوّابةٌ من ٤٢١ فحص وحدة و١٣٢ فحص متصفّح تمرّ قبل كلّ دفعة. بيئةٌ ثانية تضاعف ما تُدير | يوم يدخل **مالٌ حقيقيّ** — عندها تحتاج مكانًا تجرّب فيه ليس فيه مال الناس |
| **إصلاح عائق النسخة الواحدة** (بثٌّ مشترك + حدّ معدّل مشترك) | مكسبُه نشرٌ بلا انقطاع وبقاءٌ عند السقوط، لا حملٌ أكبر — وحركتُك بعيدةٌ عن ذلك | يوم يصير انقطاعُ ساعةٍ يكلّفك صفقات. ويبقى **القفل (٦) مستثنًى** فهو يُنفَّذ الآن |
| **الانتقال إلى AWS / جوجل / علي بابا** | قرارُ صاحب المنصّة: يقع بعد فترة، **ويحمل معه تفعيل النسخ الاحتياطيّ**. وحتى وقوعه تبقى المنصّة على الـVPS بنسخةٍ واحدة | بقرارٍ منه — لا بعلامةٍ تقنية |
| **مفتاح طريقة التحصيل** | أُجّل في ٥ سبتمبر ٢٠٢٦ بعد الاتّفاق على شكله — ثلاثة شروطٍ وسؤالٌ مفتوح عن دورة حياة الصفقة | قبل البدء يلزم إقرار دورة الحياة البديلة |

---

## الترتيب مختصرًا

```
الآن          🎯 **نشرةٌ ساقطة** — والسبب **غيرُ معروف**: لا ذاكرة ولا قرص (§١١)
بعدها         ربطُ R2 — الكود جاهز، وينتظر حاويةً ونطاقًا منك
ثمّ           ١٠ تذبذبُ البوّابة — سقطتان تتبدّلان، ولا تمنعان نشرًا
تمّ ✅        الستوريز والبنرات وطبقة التخزين · سلاسة التصفّح · هامشُ الخطّ
آخرًا         ٨ الروابط العميقة — **بقرارك**: تُترك إلى آخر شيء
لاحقًا        ٧ ما بقي: CSRF · RLS · تحقّق الإدارة بخطوتين
تمّ ✅        ١+٢ أداة النسخ · ٤ الترويسات · ٥ قائمة الفحص · ٦ قفل المسح
              وحدّ العنوان · فحص التبعيات · فحص الأسرار · تهيئة كلاودفلير
متى شئت      ٣ ضبط كلاودفلير — الكود جاهزٌ له
عند الانتقال   النسخ المُدار + العودة إلى لحظة (PITR)
مؤجَّل بقرار   staging · النسخ المتعدّدة · طريقة التحصيل
```

**وثغرتا ١ و٢ سُدّتا في الكود** — فلم يعد شيءٌ ينتظر كلاودفلير ليبدأ الحرس.
وضبطُه يبقى طبقةً فوق ما صار قائمًا، تُؤخذ متى شئت.

---

## ما ينتظر إشارتك

بنودٌ عملُها عندي، ولا تبدأ إلّا بكلمةٍ منك — تُشطب حين تُنجَز:

| الإشارة | ما أفعله عندها |
|---|---|
| ~~«ابدأ بالنسخ»~~ | ✅ **تمّ** — الأداة مبنيّةٌ ومُثبَتة، وبقي تشغيلُها على السيرفر |
| «ضبطتُ كلاودفلير» | أقيس بنفسي: ترقية `/ws` بين جهازين، و`cf-cache-status` على HTML والملفّات الساكنة (البند ٣) |
| ~~«ابدأ بالترويسات»~~ | ✅ **تمّ** — سبعٌ + `CSP` بـ`nonce`، وخمسةُ فحوصٍ تحرسها |
| «ابدأ بالقفل» | `pg_try_advisory_lock` على المسح، وفحصٌ يُشغّل مسحين متوازيين (البند ٦) |
| «ابدأ بالتطبيق» | أسرار التوقيع، ثمّ بناءٌ موقَّع إلى TestFlight وPlay — الحسابات عندك |
| «كمل الأمن» | ما بقي من ٧: CSRF ثمّ تحقّق الإدارة بخطوتين ثمّ RLS |
