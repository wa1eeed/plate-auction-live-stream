# المهامّ

> ما اتُّفق على عمله وترتيبُه وسببُه — يُحدَّث مع كلّ بندٍ يُنجَز.
> آخر تحديث: ٢٠ سبتمبر ٢٠٢٦

## القرار الذي يحكم هذا الملفّ

**النسخة القائمة على `mazad.nx.sa` هي الإنتاج**، وتدخلها بياناتٌ حقيقية خلال
أيّام. لا بيئة staging الآن، ولا انتقال إلى AWS أو جوجل.

وهذا يقلب ترتيب الأولويّات: ما كان «يُؤجَّل بلا ضرر» وهو يحمل حساباتِ تجربة،
يصير قبل دخول المال **شرطًا**. فالبنود مرتّبةٌ بهذا الحدّ لا بصعوبتها.

---

# أ — قبل أن يدخل مالٌ حقيقيّ

## ١. نسخٌ احتياطيّ يخرج من السيرفر ⬛ الأهمّ

**المشكلة اليوم**: قرصٌ واحد في سيرفرٍ واحد يحمل كلّ شيء — المستخدمين
والمحافظ والقيود. يتلف، فلا شيء يُستعاد منه.

**العمل**: من لوحة خدمة القاعدة في كوليفاي، جدولةُ نسخٍ يوميّ إلى تخزينٍ
**خارج السيرفر** (S3 أو ما يكافئه). نسخةٌ تبقى على القرص نفسه ليست نسخة.

| | |
|---|---|
| من ينفّذ | **أنت** — كوليفاي، بلا تعديل كود |
| الكلفة | ربع ساعة |
| الدليل على الإنجاز | ملفُّ نسخةٍ بتاريخ اليوم موجودٌ في التخزين الخارجيّ |

## ٢. استعادةٌ مُجرَّبة مرّةً واحدة

**لماذا بندٌ مستقلّ**: نسخةٌ لم تُستعَد قطّ ليست نسخة، بل ظنٌّ بها. والأعطال
المعتادة — نسخةٌ فارغة، صلاحيات ناقصة، إصدار بوستجرس مختلف — لا تظهر إلّا
عند أوّل استعادة، وأسوأ وقتٍ لاكتشافها هو يوم الكارثة.

**العمل**: أنزِل نسخة، استعِدها في قاعدةٍ جانبية، وعُدَّ اللوحات فيها.

> ⏸ **معلَّقٌ على البند ١** — لا استعادة قبل أن تُوجد نسخة.
>
> **والتسليم بيننا**: متى فعّلتَ النسخ الاحتياطيّ وقلتَ لي، أكتب لك أوامر
> الاستعادة التجريبية جاهزةً للصقّ — تنزيلُ النسخة، وإنشاء قاعدةٍ جانبية،
> والاستعادة فيها، وعدُّ اللوحات للمطابقة.

| | |
|---|---|
| من ينفّذ | **أنت** تُشغّل، و**أنا** أكتب الأوامر عند إشارتك |
| الكلفة | نصف ساعة، مرّةً واحدة |
| الدليل | عددُ اللوحات في القاعدة المستعادة = عددُها في الحيّة |

## ٣. كلاودفلير أمام المنصّة (وهو الـCDN نفسه)

**قيمته ليست السرعة وحدها.** قِسنا الكود: حدُّ المعدّل مبنيٌّ على **البريد
ومعرّف المستخدم** لا على عنوان الشبكة —

```
rateLimit(`login:${body.email}`, 8, 60_000)
```

فمن يهاجم بثمانية آلاف بريدٍ مختلف **لا يصطدم بحدٍّ أصلًا**. وهذه ثغرةٌ لا
يسدّها إلّا حدٌّ فوق طبقة الشبكة — وهو ما يعطيه كلاودفلير بلا سطرِ كود.

**ما يُضبط**:

- [ ] DNS عبر كلاودفلير، والسحابة **برتقالية** (proxied)
- [ ] `SSL/TLS` على **Full (strict)** — و«Flexible» تجعل الوصلة بين كلاودفلير
      وسيرفرك بلا تشفير، وهي أسوأ من لا شيء لأنّها تُظهر قفلًا كاذبًا
- [ ] `Always Use HTTPS`
- [ ] قواعد WAF المُدارة
- [ ] **حدّ معدّل على `/api/auth/*` و`/api/admin/*`** — وهو المقصد الأوّل
- [ ] `Bot Fight Mode`

**وفخّان يجب التحقّق منهما بعد التفعيل**:

1. **`/ws` لا ينكسر.** كلاودفلير يدعم WebSocket، لكن لا تُصدّق — افتح صفحة
   مزادٍ من جهازين وزايد، فإن لم يتحرّك السعر عند الثاني فالترقية محجوبة.
2. **لا تُخزَّن صفحةٌ فيها جلسة.** تحقّق أنّ `cf-cache-status` على صفحات
   HTML = `DYNAMIC`، وعلى `/_next/static/*` = `HIT`. وصفحةٌ مخزَّنة لمستخدمٍ
   تُعرض لآخر تسريبُ حساب.

| | |
|---|---|
| من ينفّذ | **أنت** — لوحة كلاودفلير، وأنا أتحقّق بعدها بالقياس |
| الكلفة | ساعة |
| الدليل | مزايدةٌ تصل بين جهازين · `cf-cache-status` صحيح على الاثنين |

## ٤. ترويسات الأمان — المنصّة اليوم بلا واحدة

**قِسناه**: `next.config.mjs` لا يحوي `headers()`، ولا يوجد `middleware`. فلا
`HSTS` ولا `X-Frame-Options` ولا `X-Content-Type-Options` ولا `Referrer-Policy`
ولا `CSP`.

| الترويسة | ما تمنع |
|---|---|
| `Strict-Transport-Security` | هبوطًا إلى HTTP يُمكّن من الاعتراض |
| `X-Frame-Options` / `frame-ancestors` | وضعَ المنصّة في إطارٍ لسرقة النقرات |
| `X-Content-Type-Options: nosniff` | تخمينَ المتصفّح لنوع ملفٍّ مرفوع |
| `Referrer-Policy` | تسريبَ مسارٍ داخليّ إلى موقعٍ خارجيّ |
| `Permissions-Policy` | طلبَ كاميرا أو موقعٍ من كودٍ دخيل |
| `Content-Security-Policy` | **تنفيذَ سكربتٍ حُقن في الصفحة** — الأقوى والأصعب |

> الخمسة الأولى سطورٌ في `next.config.mjs`. أمّا `CSP` فيحتاج `middleware`
> يولّد `nonce` لكلّ طلب، لأنّ Next يضع سكربتاتٍ سطرية. وهي **أهمّها** ولا
> تُختصر: بلا CSP، ثغرةُ XSS واحدة تُساوي حساب أدمن.

| | |
|---|---|
| من ينفّذ | **أنا** — كود |
| الكلفة | الخمس البسيطة: ساعة · CSP: نصف يوم بفحوصه |
| الدليل | فحصٌ آليّ يطلب الصفحة ويتحقّق من كلّ ترويسة — يسقط إن حُذفت |

## ٥. قائمة الفحص قبل الإنتاج — تُنفَّذ على الحيّ

موجودة في [`docs/security.md §14`](docs/security.md) و[`docs/deployment.md §10`](docs/deployment.md)،
ولم تُنفَّذ على النشرة الحيّة بندًا بندًا:

- [ ] `SESSION_SECRET` عشوائيٌّ طويل خاصٌّ بهذه البيئة
- [ ] `DEMO_MODE=false` — ولا بيانات ديمو ظاهرة
- [ ] `/api/internal/sweep` لا يُجاب من الإنترنت العامّ
- [ ] `NEXT_PUBLIC_APP_URL` = النطاق الحقيقيّ
- [ ] حدود المعدّل مضبوطة على الحمل المتوقَّع
- [ ] بيانات الأدمن التجريبية **بُدِّلت** بكلمةٍ حقيقية

| | |
|---|---|
| من ينفّذ | **أنا أقيس**، وأنت تبدّل ما في كوليفاي |
| الكلفة | ساعة |

---

# ب — بعد دخول البيانات مباشرةً

## ٦. قفلٌ على المسح الدوريّ

**الثغرة**: `finalizeDueAuctions` تقرأ ثمّ تكتب بلا قفل، وحارسُها
`existing.length === 0` فحصٌ ثمّ فعل. ونسختان من التطبيق تمرّان به معًا
فتُنشئان **صفقتين لمزادٍ واحد**. ولا تمسكها فرادةُ `orders` لأنّها على
`(buyerId, clientRequestId)` والمسح لا يمرّر `clientRequestId`.

**ولماذا الآن وأنت على نسخةٍ واحدة؟** لأنّ ما يحميك **عُرفٌ في التوثيق لا
حارسٌ في الكود**. يكفي أن تُضبط نسختان في كوليفاي يومًا، فتظهر صفقاتٌ مكرّرة
بلا رسالة خطأ واحدة — وفيها مال.

**العلاج**: `pg_try_advisory_lock` — من ظفر به مسح، والبقية تتخطّى.

| | |
|---|---|
| من ينفّذ | **أنا** |
| الكلفة | ساعة |
| الدليل | فحصٌ يُشغّل مسحين متوازيين على مزادٍ منتهٍ فتنتج صفقةٌ **واحدة** |

## ٧. مراجعةٌ أمنية شاملة

ما وجدتُه بالقياس حتى الآن، مرتّبًا بالخطر:

| # | الثغرة | الحال | العلاج |
|---|---|---|---|
| ١ | **لا ترويسات أمان** | لا `headers()` ولا `middleware` | البند ٤ |
| ٢ | **حدّ المعدّل بالبريد لا بالشبكة** | ألفُ بريدٍ = ألفُ محاولة | كلاودفلير (٣) + طبقةٌ في الكود |
| ٣ | **لا رمز CSRF صريح** | يُعتمَد على `SameSite=Lax` وحده | رمزٌ مزدوج على الطلبات المغيِّرة |
| ٤ | **لا فحص ثغراتٍ في التبعيات** | `ci.yml` لا يُشغّل `pnpm audit` | خطوةٌ في البوّابة تُسقطها على ثغرةٍ عالية |
| ٥ | **لا فحص أسرارٍ مسرَّبة** | لا حارس يمنع دفع مفتاحٍ سهوًا | `gitleaks` في البوّابة |
| ٦ | **لا RLS في القاعدة** | طبقة حمايةٍ ثانية غائبة، والخدمة وحدها الحارس | سياسات RLS |
| ٧ | **لوحة الإدارة بعاملٍ واحد** | بريدٌ وكلمة فقط | تحقّقٌ بخطوتين، أو حصرٌ بعناوين معروفة |

**وثلاثةٌ تُفحص ولا تُفترض** — بمحاولةِ اختراقٍ فعلية لا بقراءة كود:

- **السعر الاحتياطيّ**: ألّا يبلغ حمولةَ المشاهد في أيّ مسار — صفحة، API،
  WebSocket، أو حقلٍ منسيّ في استجابة.
- **أرقام الجوال**: ألّا تظهر لغير طرفَي الصفقة.
- **الصلاحيات أفقيًّا**: أن يُجرَّب مستخدمٌ يطلب مواردَ مستخدمٍ آخر بمعرّفه —
  محفظته، صفقاته، عرابينه.

| | |
|---|---|
| من ينفّذ | **أنا** — والبنود ١ و٤ و٥ آليّة تُضاف للبوّابة فتحرس دائمًا |
| الكلفة | ٢–٣ أيّام للمراجعة كاملةً، والبنود مستقلّةٌ تُنفَّذ بالترتيب |
| الدليل | فحوصٌ في المجموعة تسقط عند كلّ ثغرةٍ منها، لا تقريرٌ يُقرأ مرّة |

---

# ج — مؤجَّلٌ بقرار

| البند | لماذا أُجّل | متى يُعاد النظر |
|---|---|---|
| **بيئة staging** | مستخدموك حساباتُ تجربة، وبوّابةٌ من ٤٢١ فحص وحدة و١٣٢ فحص متصفّح تمرّ قبل كلّ دفعة. بيئةٌ ثانية تضاعف ما تُدير | يوم يدخل **مالٌ حقيقيّ** — عندها تحتاج مكانًا تجرّب فيه ليس فيه مال الناس |
| **إصلاح عائق النسخة الواحدة** (بثٌّ مشترك + حدّ معدّل مشترك) | مكسبُه نشرٌ بلا انقطاع وبقاءٌ عند السقوط، لا حملٌ أكبر — وحركتُك بعيدةٌ عن ذلك | يوم يصير انقطاعُ ساعةٍ يكلّفك صفقات. ويبقى **القفل (٦) مستثنًى** فهو يُنفَّذ الآن |
| **الانتقال إلى AWS / جوجل** | تشتري ثلاثةً لتأخذ واحدًا، والواحد — النسخ الاحتياطيّ — تأخذه حيث أنت بلا انتقال | يوم يختنق السيرفر فعلًا |
| **مفتاح طريقة التحصيل** | أُجّل في ٥ سبتمبر ٢٠٢٦ بعد الاتّفاق على شكله — ثلاثة شروطٍ وسؤالٌ مفتوح عن دورة حياة الصفقة | قبل البدء يلزم إقرار دورة الحياة البديلة |

---

## الترتيب مختصرًا

```
هذا الأسبوع   ١ نسخ احتياطي ← ٢ استعادة مجرَّبة ← ٣ كلاودفلير ← ٤ ترويسات ← ٥ قائمة الفحص
بعده مباشرةً   ٦ قفل المسح ← ٧ المراجعة الأمنية
مؤجَّل بقرار   staging · النسخ المتعدّدة · السحابة · طريقة التحصيل
```

**والبند ١ قبل كلّ شيء.** ما سواه يمنع ضررًا محتملًا؛ وهو وحده يُنقذ ممّا
لا رجعة منه.

---

## ما ينتظر إشارتك

بنودٌ عملُها عندي، ولا تبدأ إلّا بكلمةٍ منك — تُشطب حين تُنجَز:

| الإشارة | ما أفعله عندها |
|---|---|
| **«فعّلتُ النسخ الاحتياطيّ»** | أكتب أوامر الاستعادة التجريبية (البند ٢) جاهزةً للصقّ |
| «ضبطتُ كلاودفلير» | أقيس بنفسي: ترقية `/ws` بين جهازين، و`cf-cache-status` على HTML والملفّات الساكنة (البند ٣) |
| «ابدأ بالترويسات» | الخمس البسيطة ثمّ `CSP` بـ`middleware` و`nonce`، وفحصٌ آليّ يحرسها (البند ٤) |
| «ابدأ بالقفل» | `pg_try_advisory_lock` على المسح، وفحصٌ يُشغّل مسحين متوازيين (البند ٦) |
| «ابدأ بالمراجعة الأمنية» | البنود الآلية أوّلًا — `pnpm audit` و`gitleaks` في البوّابة — ثمّ الباقي بالترتيب (البند ٧) |
