# صقل الغلاف الأصيل — الخطّة

> فُحص الوضع القائم في ٢٢ سبتمبر ٢٠٢٦ قبل أيّ تعديل. وما هو منفَّذٌ **لا
> يُعاد بناؤه**: الطلب صقلٌ لا إعادة تصميم.

## ١. الوضع الحالي — مقيسًا لا مفترَضًا

### ✅ منفَّذٌ وسليم — لا يُلمس

| المجال | الحال |
|---|---|
| **إعداد Capacitor** | `server.url` إلى `mazad.nx.sa` · `cleartext: false` · `allowNavigation` للنطاق وحده · `launchAutoHide: false` |
| **الشاشة الافتتاحية** | تُخفى **بأمرٍ بعد أوّل رسم** لا بمؤقّت — فلا شاشةٌ بيضاء ولا انتظارٌ بلا سبب |
| **رجوع أندرويد** | مربوطٌ بزرّ الجهاز: يتبع التاريخ، والجذرُ **يُصغَّر ولا يُغلق** |
| **دورة الحياة** | `appStateChange` → `router.refresh()` — الخادم أصدق ممّا على الشاشة |
| **الروابط العميقة** | `appUrlOpen` يأخذ **المسار وحده** ويرفض ما جاء من نطاقٍ آخر |
| **الدفع** | تسجيلٌ عند كلّ إقلاعٍ ما دام الإذن قائمًا · الضغط يفتح الوجهة · APNs مباشرةً لـiOS وFCM لأندرويد |
| **اللحظية** | تراجعٌ أُسّيّ بنثر · كشفُ فجواتٍ بالتسلسل → مزامنةٌ كاملة · مزامنةٌ عند كلّ اتّصال وعند اليقظة و`online` · نبضةٌ تكشف المقبس الميّت · سقوطٌ إلى جلبٍ دوريّ بعد ٣ محاولات · تنظيفٌ كامل عند التفكيك · حارسُ `stale()` يمنع المستمعين المكرّرين |
| **أمان المزايدة** | `inFlight` يمنع التكرار · الزرّ يُعطَّل · **لا نجاحَ متوقَّعًا** — لا يُعلن إلّا بعد تأكيد الخادم · **لا طابور للمزايدات** · `clientRequestId` بفرادةٍ في القاعدة |
| **الاهتزاز** | ثلاثة أنماط، **في مواضع الأثر وحدها** — لا مع كلّ تحديث |
| **المشاركة الأصيلة** | `share-button.tsx` |
| **الارتداد المطاطيّ** | `overscroll-behavior-y: contain` |
| **الإشعارات العاجلة** | `interruption-level: time-sensitive` لثلاثة أنواع |

> **الملاحة السفلية غير موجودة أصلًا** — والتنقّل بهيدرٍ لاصقٍ وقائمةٍ منسدلة.
> فلا شيء يُصلَح هنا، ولا تُضاف ملاحةٌ سفلية: إضافتُها **إعادةُ تصميم**، وهي
> خارج نطاق هذه المرحلة صراحةً.

### ❌ الثغرات — وهي التي تجعله يبدو أقلّ أصالة

| # | الثغرة | القياس |
|---|---|---|
| ١ | **`viewport-fit: cover` غائب** | `layout.tsx` فيه `width` و`initialScale` ولا ثالث |
| ٢ | **الهيدر لا يحسب الشقّ** | `sticky top-0` بلا `env(safe-area-inset-top)` — فيتراكم تحت الساعة على آيفون ذي جزيرة |
| ٣ | **شريط الحالة بأسلوبٍ ثابت** | `Style.Light` مرّةً عند الإقلاع، لا يتبع محتوى الشاشة |
| ٤ | **لا مراقبة شبكة** | `@capacitor/network` غير مثبَّت؛ والاعتماد على فشل الطلب و`navigator.onLine` |
| ٥ | **لا واجهة انقطاع** | الغلاف يعرض صفحة خطأ المتصفّح |
| ٦ | **لا شريط «غير متّصل»** | الانقطاع لا يُعلَن، فيُظنّ العطبُ في المنصّة |
| ٧ | **مزايدةٌ ناجحة تُعرض فاشلة** ⚠ | انقطاعٌ **في أثناء** الإرسال: `catch` يعرض خطأً **ولا يُعيد الجلب** — والمزايدة قد وقعت عند الخادم |
| ٨ | **رسالةٌ واحدة لكلّ خطأ شبكة** | لا تمييز بين انقطاعٍ ومهلةٍ وعطب خادم |
| ٩ | **الشارة لا تُصفَّر على iOS** | تستعمل `navigator.setAppBadge` — وهي **غير مدعومة** في غلاف iOS، فيبقى الرقم القديم |
| ١٠ | **لا هيدر تطبيقٍ بزرّ رجوع** | وiOS بلا زرٍّ عتاديّ — فالصفحات الداخلية بلا مخرج إلّا الإيماءة |
| ١١ | **لوحة المفاتيح** | لا معالجة — وقد تغطّي حقل المبلغ على iOS |
| ١٢ | **لا مفاتيح في لوحة الإدارة** | للصوت والاهتزاز ونصّ الانقطاع |

## ٢. لماذا تُشعر بأنّه «موقعٌ في نافذة»

ثلاثةٌ من الاثنتي عشرة هي الحاسمة في الإحساس:

1. **الشقّ** — تطبيقٌ أصيل لا يضع عنوانه تحت الساعة. وهذه أوّل ما يُرى.
2. **الانقطاع** — صفحةُ خطأ المتصفّح تقول «هذا موقع» بأعلى صوت.
3. **زرّ الرجوع** — على iOS لا بديل عنه إلّا إيماءةٌ لا يعرفها كلّ مستخدم.

وواحدةٌ حاسمةٌ في **الصحّة** لا الإحساس: **السابعة**. مزايدةٌ وقعت وتُعرض
فاشلةً تجعل صاحبها يزايد ثانيًا — وهي مالٌ لا شكل.

## ٣. التعديلات — بالترتيب

### المرحلة أ — المناطق الآمنة وشريط الحالة

- `viewport-fit: cover` في `layout.tsx`
- متغيّرات CSS للمناطق الآمنة في `globals.css`، وتطبيقها على: الهيدر،
  والـtoaster، والأوراق السفلية، وشريط المزايدة (الأخيران معالَجان جزئيًّا)
- شريط الحالة: **شفّافٌ متراكب** على أندرويد، وأسلوبٌ يتبع السمة
- **ولا يُمسّ الويب**: الإضافات كلُّها `env()` وهي صفرٌ في المتصفّح

### المرحلة ب — الشبكة والانقطاع

- `@capacitor/network` + مزوّدٌ يوحّد حالة الشبكة للويب والغلاف
- شريط «غير متّصل» يظهر ويختفي من تلقائه
- شاشة انقطاعٍ بالهويّة تحلّ محلّ صفحة المتصفّح — في `capacitor/www` و
  كصفحةٍ داخلية
- «تم استعادة الاتّصال» عند العودة

### المرحلة ج — سلامة المزايدة والأخطاء

- **مصالحةُ حالةٍ عند انقطاعٍ في أثناء الإرسال**: النتيجة «غير مؤكّدة»،
  فيُجلب حال المزاد من الخادم ولا يُعاد الإرسال
- تمييزُ الأخطاء: انقطاع · مهلة · غير مصرَّح · عطب خادم · مزاد مغلق ·
  مزايدة مرفوضة
- منعُ المزايدة وهي مُنقطعة بنصٍّ صريح، **بلا طابور**

### المرحلة د — هيدر التطبيق والشارة ولوحة المفاتيح

- زرّ رجوعٍ في الهيدر **للغلاف وحده** على الصفحات الداخلية، ومسارٌ احتياطيّ
  لمن جاء من رابطٍ عميق
- الشارة عبر جسر الغلاف لا عبر واجهة الويب
- لوحة المفاتيح: تمرير الحقل إلى المرأى عند التركيز

### المرحلة هـ — لوحة الإدارة والتوثيق

- مفاتيح: الصوت، والاهتزاز، ونصّ الانقطاع
- `NATIVE_SHELL_POLISH_REPORT.md`

## ٤. الملفّات التي تتغيّر

```
src/app/layout.tsx                  viewport-fit
src/app/globals.css                 متغيّرات المناطق الآمنة
src/components/layout/site-header.tsx   حشوة الشقّ + زرّ الرجوع للغلاف
src/components/ui/toaster.tsx        حشوة الأسفل
src/components/layout/native-shell.tsx  شريط الحالة + الشبكة + الشارة
src/components/layout/network-banner.tsx        ← جديد
src/lib/hooks/use-network.ts                    ← جديد
src/lib/api-error.ts                            ← جديد
src/components/market/auction-bid-box.tsx  المصالحة + الأخطاء
capacitor/www/index.html             شاشة الانقطاع
capacitor.config.ts                  شريط الحالة + لوحة المفاتيح
package.json                         @capacitor/network · @capacitor/keyboard
src/lib/domain/types.ts              مفاتيح لوحة الإدارة
```

## ٥. المخاطر

| الخطر | الحدّ منه |
|---|---|
| **المناطق الآمنة تُفسد الويب** | `env()` تساوي صفرًا في المتصفّح — وفحصٌ يقيس الهيدر على الويب |
| **شريط حالةٍ متراكب يُخفي المحتوى** | الحشوة تُضاف قبل التراكب في الإيداع نفسه |
| **إضافةُ إضافتين تكسر البناء** | `cap sync` في البوّابة، وبناءُ الصورة يمرّ قبل الدفع |
| **مصالحةُ الحالة تُظهر نجاحًا كاذبًا** | لا تُعلن نجاحًا أبدًا: تجلب الحال وتقول «تحقّق من المزايدة» |
| **شريط الانقطاع يظهر خطأً** | لا يُعتمد `navigator.onLine` وحده: تُقدَّم حالةُ الغلاف عليه |
| **إعادة تصميمٍ بالخطأ** | لا مكوّن يُستبدل؛ الإضافات حشواتٌ وأغلفةٌ وسلوك |

## ٦. ما لا يُنفَّذ — وسببه

| | لماذا |
|---|---|
| **ملاحةٌ سفلية** | غير موجودة، وإضافتُها إعادةُ تصميم — وهي مستثناةٌ في الطلب |
| **تخزين صفحات المزاد للعمل دون اتّصال** | سعرٌ قديم يُبنى عليه أخطرُ من لا سعر — القرار موثَّقٌ في `public/sw.js` بقياسه |
| **طابور مزايداتٍ مؤجّلة** | ممنوعٌ في الطلب صراحةً، وهو صواب |
| **إعادة تصميم شاشة** | خارج النطاق |
