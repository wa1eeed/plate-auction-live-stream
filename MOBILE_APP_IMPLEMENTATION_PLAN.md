# خطّة تطبيق الجوال — تحليلٌ وقرارٌ مطلوب

> **الخلاصة قبل التفصيل:** الطلب يفترض «React Web + Backend منفصل بـNode»،
> والمشروع ليس كذلك. هو **Next.js 15 App Router** يُصيَّر على الخادم، وخادمُه
> هو الواجهةُ والـAPI معًا. وCapacitor يشترط مبنًى ساكنًا يُحزَم داخل التطبيق
> — ولا يوجد في هذا المشروع مبنًى كهذا، ولا يُستخرج منه إلّا بإعادة بناءٍ
> ممنوعةٍ في الطلب نفسه.
>
> فالتنفيذ المباشر متعذّر، والقرار قرارك بين ثلاثة مسارات (القسم ٥).
>
> **القرار المتَّخذ (٢٠ سبتمبر ٢٠٢٦):** المسار **(أ)** — غلاف Capacitor أصيل
> يفتح الخادم، بطبقةٍ أصيلةٍ كاملة. والمراحل ١–٦ تبدأ فورًا لأنّها لا تتعلّق
> بالقرار.

---

## ١. تحليل البنية الحالية

فُحص المشروع كلُّه. ما يلي **مقيسٌ لا مقدَّر**:

| البند | الواقع |
| --- | --- |
| الإطار | Next.js `15.1.3` · React `19` · TypeScript صارم |
| التوجيه | **App Router** (`src/app`) — لا `react-router` ولا SPA |
| الصفحات | **٤٥** صفحة، **صفرٌ** منها مكوّن عميل |
| `force-dynamic` | **٨٩** ملفًّا يمنع التوليد الساكن صراحةً |
| الـBackend | **٤٥** مسار API **داخل نفس التطبيق** — لا خادم Node منفصل |
| قراءة الجلسة على الخادم | **٨٠** ملفًّا |
| مكوّنات العميل | ٨٥ من ١٧١ (أغلبها عناصر واجهة لا صفحات) |
| المصادقة | كوكي `httpOnly` + `sameSite=lax`، تُقرأ بـ`cookies()` على الخادم |
| قاعدة البيانات | مخزنٌ في الذاكرة (`MemoryStore`) + لقطة إعدادات على القرص |
| اللحظية | `ws` في `server.mjs` — خادمٌ مخصّص يجمع Next والمقابس |
| الخادم | `node server.mjs` — ليس `next start` |

### ما هو مبنيٌّ فعلًا من قائمة «القدرات الأصيلة»

| الطلب | الحال اليوم |
| --- | --- |
| PWA · بيان · عامل خدمة · تثبيت | ✅ منشور |
| إشعارات دفع (ويب) + حذف الاشتراك الميّت | ✅ منشور |
| مركز إشعارات في قاعدة البيانات | ✅ قائم (`notifications` بأنواعه ووجهاته) |
| مزامنةٌ عند العودة من الخلفية | ✅ `use-realtime.ts:200` — `visibilitychange` يُعيد الجلب |
| ورقة المشاركة الأصلية | ✅ `navigator.share` |
| أصوات (مزايدة · تجاوز · فوز · تكّة · سوم) | ✅ مولَّدة بـWeb Audio، ولها مفتاح |
| عدّاد تنازليّ · RTL · مناطق آمنة | ✅ قائمة |
| منع الضغط المكرّر على المزايدة | ✅ `inFlight` في صندوق المزايدة |

**نحو ٦٠٪ ممّا طلبتَه في قائمة «Native Feel» موجودٌ ويعمل.** الناقص أصيلًا:
APNs/FCM، والروابط الكونية، وشارة الأيقونة، والاهتزاز، وشاشة الإقلاع الأصيلة،
وزرّ الرجوع في أندرويد.

---

## ٢. ما يُعاد استعماله كما هو

- **الـBackend كلّه** — ٤٥ مسار API، بلا حرفٍ واحد.
- **محرّك المزاد والعرابين والعمولة** — منطقٌ خادميّ بحت.
- **اللحظية** (`ws`) — تعمل من أيّ عميل يفتح `wss://`.
- **٨٥ مكوّن عميل** — البطاقات واللوحة والعدّادات والنماذج.
- **التصميم كلّه** — Tailwind ورموزه، بلا إعادة تصميم.
- **مركز الإشعارات وقاعدته**.

## ٣. ما يحتاج تعديلًا للجوال

| البند | العمل |
| --- | --- |
| تسجيل الأجهزة | تعميم `pushSubscriptions` إلى `user_devices` بـ`platform` و`appVersion` |
| الإذن | شاشةُ تمهيدٍ قبل طلب الإذن الأصيل (اليوم: مفتاحٌ مباشر) |
| الروابط العميقة | المسارات قائمة (`/market/[id]`, `/account/*`) — يلزم ربطها بالإشعار |
| زرّ الرجوع | لا وجود له في الويب — يلزم `App.addListener('backButton')` |
| الشارة | `navigator.setAppBadge` في الويب، وإضافة أصيلة في الغلاف |
| الاهتزاز | `navigator.vibrate` (أندرويد/ويب)، و`Haptics` في الغلاف |
| قسم «Mobile App» في الإدارة | جديد: قوالب الإشعارات، ورايات، ومعلومات الإصدار |

## ٤. العائق الحقيقي

Capacitor يحمّل `webDir` — مجلّدًا **ساكنًا** من HTML و JS يُحزَم داخل الـIPA
والـAPK. وهذا المشروع لا يُنتج مجلّدًا كهذا:

1. **٤٥ صفحةً كلُّها مكوّنات خادم.** تُصيَّر في العملية بطلبٍ من قاعدة
   البيانات. `output: 'export'` يرفضها.
2. **٨٩ ملفًّا يعلن `force-dynamic`.** أي: «لا تولّدني ساكنًا» صراحةً.
3. **الجلسة تُقرأ على الخادم في ٨٠ ملفًّا.** الكوكي `httpOnly` — لا يقرؤه
   جافاسكربت أصلًا، وهو ما يحميه. وفي غلافٍ أصيل يصير الأصل `capacitor://`
   فتصير الكوكي عابرةَ موقعٍ ولا تُرسَل مع `sameSite=lax`.
4. **الـAPI داخل التطبيق نفسه.** لا يوجد «Backend منفصل» يتّصل به الغلاف.

**فتحويله إلى SPA ساكن = إعادة كتابة ٤٥ صفحة، ونقل ٨٠ موضع مصادقة إلى العميل،
وتبديل الكوكي بِرمزٍ يُحفظ في الجهاز.** وهذا بالضبط ما منعتَه: «ممنوع إعادة
بناء المشروع من الصفر».

---

## ٥. المسارات الثلاثة — القرار لك

### (أ) Capacitor بغلافٍ أصيل يفتح الخادم — **المُوصى به**

`server.url` في `capacitor.config` يشير إلى `https://mazad.nx.sa`، والغلاف
يحمل الطبقة الأصيلة كاملةً.

**ليس «WebView فقط»**: الغلاف يضمّ APNs/FCM، والروابط الكونية، والشارة،
والاهتزاز، وشاشة الإقلاع، وزرّ الرجوع، وورقة المشاركة — ويتخاطب مع الويب عبر
جسرٍ برمجيّ. وهو ما تفعله منصّات كبرى بواجهاتٍ خادمية.

| ✅ | ⚠️ |
| --- | --- |
| قاعدة كودٍ واحدة حقًّا | يخالف شرطك «لا WebView» بحرفه |
| بلا إعادة بناء | يحتاج إنترنت دائمًا (والمزاد يحتاجه أصلًا) |
| التحديث بلا إصدار متجر | خطر **App Store 4.2** — تُخفَّف بالطبقة الأصيلة |
| ٢–٣ أسابيع | |

### (ب) عميلٌ ساكن جديد داخل المستودع

هدفٌ ثانٍ (Vite) يعيد استعمال المكوّنات الـ٨٥، ويعيد كتابة الصفحات الـ٤٥
عميلًا، ومصادقةً برمزٍ بدل الكوكي.

| ✅ | ⚠️ |
| --- | --- |
| يطابق شرطك حرفًا بحرف | **قاعدتا كودٍ للواجهة** — وهو ما منعتَه |
| يعمل بلا شبكة جزئيًّا | ٦–١٠ أسابيع |
| | كلُّ ميزةٍ جديدة تُبنى مرّتين إلى الأبد |
| | سطح أمانٍ جديد: رمزٌ يُحفظ في الجهاز بدل `httpOnly` |

### (ج) إتمام الـPWA بدل المتجرين

الاستمرار على ما نُشر: تثبيتٌ من المتصفّح، ودفعٌ يعمل (أندرويد وسطح المكتب،
وiOS متى ثُبّتت).

| ✅ | ⚠️ |
| --- | --- |
| أسبوعٌ واحد | لا وجود في App Store ولا Google Play |
| بلا مراجعة متجر | دفع iOS يشترط التثبيت اليدويّ |

> **رأيي:** (أ). هي الوحيدة التي تحقّق «قاعدة كودٍ واحدة» و«لا إعادة بناء»
> و«نشرٌ في المتجرين» معًا. و(ب) تشتري الحرفيّة بثمنِ ما جئتَ تتجنّبه.

---

## ٦. المخاطر

| الخطر | التخفيف |
| --- | --- |
| **App Store 4.2** يرفض الأغلفة الرقيقة | طبقةٌ أصيلة حقيقية: دفع، روابط كونية، اهتزاز، شارة، مشاركة |
| **الكوكي `httpOnly` عبر `capacitor://`** | `server.url` يُبقي الأصل واحدًا فتبقى الكوكي عاملةً كما هي |
| **المخزن في الذاكرة** | الاشتراكات **لا تُحفظ**، ولا ينفع حفظها: معرّفات المستخدمين تُولَّد عشوائيًّا كلّ إقلاع فيعود المحفوظ مربوطًا بحسابٍ لا وجود له. الهويّة والاشتراك يضيعان معًا ويعودان معًا — الصفحة تُعيد إرسال اشتراكها بلا سؤال. وثباتُهما معًا يأتي بقاعدة بيانات لا بملفّ ([pwa.md §7.3](docs/pwa.md)) |
| **أسرار في التطبيق** | لا شيء: كلّ المنطق خادميّ، والغلاف لا يحمل مفتاحًا |
| **دَين الاختبارات** | باقٍ: العامل يُستثنى من المجموعة الشاملة. قِيس على خمس تشغيلات — بلا عامل ١٣٢/١٣٢، ومعه ١٢٩–١٣١ بسقطاتٍ متبدّلة |

---

## ٧. خطّة التنفيذ (على المسار «أ»)

| # | المرحلة | تعتمد على القرار؟ |
| --- | --- | --- |
| 1 | ⚠️ دَين الاختبارات **باقٍ ومُقاس** — العامل يُستثنى من المجموعة؛ أربع سقطاتٍ صُوِّبت وبقي العَرَض ([pwa.md §4.1](docs/pwa.md)) | ❌ لا |
| 2 | تدقيق تجربة الجوال: مناطق آمنة، لوحة مفاتيح، أوراق سفلية | ❌ لا |
| 3 | ✅ `user_devices` بدل `pushSubscriptions` — بمنصّةٍ ورمزٍ ونسخةٍ ورايةِ إشعارات | ❌ لا |
| 4 | شاشة تمهيدٍ قبل طلب الإذن | ❌ لا |
| 5 | الاهتزاز والشارة عبر واجهات الويب | ❌ لا |
| 6 | قسم «Mobile App» في الإدارة + قوالب الإشعارات | ❌ لا |
| 7 | تثبيت Capacitor و`/ios` و`/android` | ✅ نعم |
| 8 | APNs + FCM وربطهما بخدمة الإشعارات الواحدة | ✅ نعم |
| 9 | الروابط الكونية وApp Links | ✅ نعم |
| 10 | شاشة الإقلاع، وشريط الحالة، وزرّ الرجوع | ✅ نعم |
| 11 | اختبارات الجهازين وقوائم الإصدار | ✅ نعم |

**المراحل ١–٦ تنفع في المسارات الثلاثة جميعًا**، فأستطيع البدء بها اليوم بلا
انتظار قرارك — وهي أسبوعٌ من العمل يفيد الويب والتطبيق معًا.

---

## ٨. ما لم يُنفَّذ بعد ولماذا

لم يُثبَّت Capacitor، ولم يُنشأ `/ios` ولا `/android`. السبب أنّ تثبيته
بـ`webDir` لا وجود له يُنتج مشروعين لا يُقلعان، ويُضاف إلى المستودع ما يُوهم
بأنّ التطبيق قائم. والاختيار بين (أ) و(ب) يغيّر كلّ حرفٍ في `capacitor.config`.
