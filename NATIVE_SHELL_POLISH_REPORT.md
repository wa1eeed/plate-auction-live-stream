# صقل الغلاف الأصيل — التقرير

> نُفِّذ في ٢٢ سبتمبر ٢٠٢٦. البوّابة: **٤٦٤** فحص وحدة · **١٥٨/١٥٨** متصفّح.
> الخطّة والفحص السابق في [`NATIVE_SHELL_POLISH_PLAN.md`](NATIVE_SHELL_POLISH_PLAN.md).

## ١. ما وُجد — والمفاجأة أنّ أكثره كان قائمًا

**فُحص أوّلًا، فتبيّن أنّ الطلب يفترض نقصًا لا وجود له.** وهذه منفَّذةٌ
وسليمةٌ ولم تُلمس:

| المجال | الحال قبل العمل |
|---|---|
| **اللحظية** | تراجعٌ أُسّيّ بنثر · كشفُ فجواتٍ بالتسلسل → مزامنةٌ كاملة · مزامنةٌ عند كلّ اتّصال وعند اليقظة و`online` · نبضةٌ تكشف المقبس الميّت · سقوطٌ إلى جلبٍ دوريّ · تنظيفٌ كامل · حارسُ `stale()` يمنع المستمعين المكرّرين |
| **أمان المزايدة** | `inFlight` · الزرّ يُعطَّل · **لا نجاحَ متوقَّعًا** · **لا طابور** · `clientRequestId` بفرادةٍ في القاعدة |
| **دورة الحياة** | `appStateChange` → `router.refresh()` |
| **رجوع أندرويد** | مربوطٌ بالجهاز، والجذرُ يُصغَّر ولا يُغلق |
| **الروابط العميقة** | المسار وحده، ورفضُ ما جاء من نطاقٍ آخر |
| **الشاشة الافتتاحية** | تُخفى بأمرٍ بعد أوّل رسم لا بمؤقّت |
| **الاهتزاز** | في مواضع الأثر وحدها |
| **المشاركة الأصيلة · الارتداد المطاطيّ** | منفَّذان |

> **ولذلك لم يُعَد بناءُ شيء.** ومن أراد «إصلاح» ما يعمل أفسده.

### الثغرات الفعلية — اثنتا عشرة

| # | ما وُجد |
|---|---|
| ١ | `viewport-fit: cover` غائب — فالمناطق الآمنة كلُّها أصفار |
| ٢ | الهيدر `sticky top-0` بلا حشوةٍ — يتراكم تحت ساعة النظام |
| ٣ | شريط الحالة بأسلوبٍ ثابت يُضبط مرّةً عند الإقلاع |
| ٤ | لا مراقبة شبكة — الاعتماد على فشل الطلب |
| ٥ | لا واجهة انقطاعٍ داخل التطبيق |
| ٦ | لا إعلانَ للانقطاع |
| ٧ | ⚠ **مزايدةٌ ناجحة تُعرض فاشلة** |
| ٨ | رسالةٌ واحدة لكلّ خطأ شبكة |
| ٩ | الشارة لا تُصفَّر على iOS |
| ١٠ | لا زرّ رجوعٍ للغلاف — وiOS بلا زرٍّ عتاديّ |
| ١١ | لوحة المفاتيح تغطّي شريط المزايدة |
| ١٢ | لا مفاتيح للصوت والاهتزاز في اللوحة |

## ٢. أخطرُها — وهي في المال لا في الشكل

**السابعة.** انقطاعٌ **في أثناء** إرسال المزايدة كان يُعرض فشلًا:

```ts
} catch {
  toast.error('تعذّر الاتصال — تحقّق من الشبكة وأعد المحاولة')
}
```

والطلب خرج ولم يعد ردُّه. **فالخادم قد سجّل المزايدة وضاع ردُّه في الطريق.**
وقولُ «فشلت» يدفع صاحبها إلى إعادةٍ تُنتج مزايدتين، ولا يُعاد الجلب فتبقى
الشاشة تُظهر سعرًا أقلّ من مزايدته هو.

### ما صار

| الحال | النصّ | الفعل |
|---|---|---|
| منقطعٌ **قبل** الإرسال | «تعذّر إرسال المزايدة لعدم وجود اتّصال» | لا يُرسل — **ولا طابور** |
| انقطاعٌ **في أثناء** الإرسال | «انقطع الاتّصال قبل أن يصل الردّ — نتحقّق من حالتك الآن» | **مصالحةٌ من الخادم**، ولا إعادة إرسال |
| 409 / 404 | رسالة الخادم | جلبٌ فوريّ — الحالة تبدّلت |
| مهلة · عطب خادم · صلاحية | نصٌّ لكلّ سبب | لا إعادةَ تلقائية للمال |

و`FAILURE_TEXT.uncertain` **لا يحوي كلمة «فشل»** — ويحرس ذلك فحصٌ صريح.

## ٣. ما نُفِّذ

### شريط الحالة

- `setOverlaysWebView({ overlay: true })` — خلفية الهيدر تمتدّ تحته فيُقرأ
  الوقت عليها. وهو **غير موجودٍ على iOS** فيُتجاهَل بلا خطأ، وiOS متراكبٌ
  أصلًا مع `viewport-fit: cover`.
- الأسلوب من `data-theme` لا ثابتًا، **و`MutationObserver` يُعيده عند تبدّل
  السمة** — فمن بدّلها ليلًا لا تختفي عنه ساعته.
- واللون `var(--color-ink-950)` — **خلفية الصفحة نفسها**، فلا تعريفَ ثانٍ
  يفترق عنها بعد أوّل تعديل.
- **وصُحّح منطقٌ كان سيخفي الساعة**: السمة الأساسية في المنصّة **داكنة**،
  و`data-theme="light"` هي التي تُضبط. فاختبار `=== 'dark'` يُخطئ حين لا
  تُضبط السمة: يحسبها فاتحةً فتصير الأيقونات داكنةً على داكن. صار
  `=== 'light'` وما عداه داكن.

### المناطق الآمنة

- `viewportFit: 'cover'` في `layout.tsx` — **وبدونها تبقى القيم أصفارًا**
- أربعةُ متغيّرات على `:root` تُقرأ حيث تلزم — لا `env()` متفرّقةٌ تُنسى في
  موضعٍ فيقع عنصرٌ تحت الساعة ولا يظهر إلّا على جهازٍ بعينه
- طُبّقت على: الهيدر · الـtoaster · شريط الانقطاع
- وكانت مطبَّقةً أصلًا على: شريط المزايدة · الأوراق السفلية

### الشبكة والانقطاع

- `@capacitor/network` + [`use-network.ts`](src/lib/hooks/use-network.ts):
  حالةٌ موحّدة للويب والغلاف، **وإضافةُ النظام مقدَّمةٌ على
  `navigator.onLine`** — فهي تقول «متّصل» لمن اتّصل بموجّهٍ بلا إنترنت
- [`network-banner.tsx`](src/components/layout/network-banner.tsx): يُعلن
  الانقطاع **ولا يُبدّل الشاشة** — ونصُّه يقول الحقيقة: «الأسعار المعروضة قد
  لا تكون الحاليّة»
- «تم استعادة الاتّصال» يظهر أربع ثوانٍ ثمّ يختفي، **ويُعاد جلبُ الصفحة**
- وحارسٌ يمنع جلبًا عند أوّل تركيب: الشرط أنّه **كان** منقطعًا
- وصفحة [`capacitor/www/index.html`](capacitor/www/index.html) صار فيها
  **إعادةُ اتّصالٍ تلقائية** على حدث `online` وبفحصٍ دوريّ — لا زرٌّ وحده

### الرجوع والشارة ولوحة المفاتيح

- [`app-back-button.tsx`](src/components/layout/app-back-button.tsx): للغلاف
  وحده، وفي غير الجذور، **ويُرسم بعد الترطيب** — فالمخرَج مشتركٌ بين الويب
  والتطبيق، ورسمُه على الخادم يُظهره في المتصفّح أو يُسقط الترطيب
- ومسارٌ احتياطيّ لمن جاء من رابطٍ عميق: `history.length > 1` أو السوق
- الشارة: جسرُ الغلاف مع واجهة الويب — `navigator.setAppBadge` **لا وجود لها
  في غلاف iOS**، فكان الرقم يبقى بعد القراءة. ومع الصفر يُمسح مركز الإشعارات
- `Keyboard: { resize: 'body' }` — و`native` يُزلق المحتوى فيخرج الهيدر
  ويبقى الحقل خلف اللوحة؛ و`body` يُصغّر النافذة فيُعاد التخطيط

### لوحة الإدارة

مفتاحان: **تكّة المزايدة** و**الاهتزاز**. وهما **يعملان** لا يُحفظان:
كابحٌ يُقرأ من سمةٍ على الجذر — كما تُقرأ السمة اللونية — فلا يُمرَّر خيطًا
من التخطيط إلى كلّ مكوّنٍ يُصدر نبضة. ويحرسه فحصٌ يُثبت السكوت عند الإطفاء.

## ٤. الملفّات

```
جديد:
  src/lib/hooks/use-network.ts
  src/lib/api-error.ts
  src/components/layout/network-banner.tsx
  src/components/layout/app-back-button.tsx
  tests/unit/api-error.test.ts
  tests/e2e/native-shell.spec.ts
  NATIVE_SHELL_POLISH_PLAN.md · NATIVE_SHELL_POLISH_REPORT.md

معدَّل:
  src/app/layout.tsx · src/app/globals.css
  src/components/layout/{site-header,native-shell,notification-bell}.tsx
  src/components/ui/toaster.tsx
  src/components/market/auction-bid-box.tsx
  src/components/admin/mobile-settings-form.tsx
  src/lib/{haptics.ts,hooks/use-sound.ts,domain/types.ts}
  src/app/api/admin/settings/mobile/route.ts
  capacitor.config.ts · capacitor/www/index.html
  .github/workflows/mobile-ios.yml · package.json
```

## ٥. الإضافات

| | لماذا |
|---|---|
| `@capacitor/network` | حالةُ الشبكة من النظام لا من فشل الطلب |
| `@capacitor/keyboard` | `resize: body` فلا تغطّي اللوحة شريط المزايدة |

**وحارسٌ في البوّابة**: `Package.swift` يولّده `cap sync` وهو **مُودَعٌ فيَبلى**.
ومزامنةٌ تسقط صامتةً تُبقي الملفّ القديم فيُبنى تطبيقٌ بلا الإضافة — وكودُنا
يتعامل مع غيابها بلطفٍ (يسقط إلى أحداث الويب) **فلا يظهر عطب: تغيب الميزة
ولا أحد يعلم**. فصار الورك‌فلو يطابق المعلَن في `package.json` بالمُولَّد في
`Package.swift` ويسقط إن نقص شيء. (جُرّب على الملفّ القديم فكشف الاثنتين.)

## ٦. ما اختُبر — وكيف

### في المتصفّح (١٥٨/١٥٨)

| الفحص | ما يثبته |
|---|---|
| لا زرّ رجوعٍ في المتصفّح | **الويب لم يتأثّر** — وهو شرطٌ صريح |
| حشوة الهيدر = `0px` | `env()` صفرٌ في المتصفّح، فالويب على حاله حرفًا بحرف |
| أربعةُ متغيّراتٍ معرَّفة | ولون الشريط يتبع رمز الخلفية |
| `viewport-fit=cover` في الوسم | وبدونها تبقى المناطق أصفارًا |
| شريطٌ عند القطع، ونصُّه يقول «قد لا تكون الحاليّة» | الانقطاع يُعلَن بصدق |
| **البطاقات باقيةٌ بعد القطع** | الصفحة لا تُمحى من تحت قارئها |
| «تم استعادة الاتّصال» عند العودة | العودة تُعلَن |

### في الوحدات (٤٦٤)

تصنيفُ الأخطاء الأحد عشر · أنّ «غير المؤكّد» لا يقول «فشل» · أنّ ما قد يكون
وقع **لا يُعاد إرساله** · وأنّ كابح الاهتزاز **يُسكِته فعلًا**.

### ما لم يُختبر آليًّا — وسببه

**ما يلزمه جهاز**: الشقّ، والجزيرة الديناميكية، وأشرطة أندرويد، ولوحة
المفاتيح، والشارة، والمشاركة، والضغط على إشعار. لا محاكيَ لهذه في متصفّح،
وXcode غير مثبَّتٍ على جهاز التطوير — **فالتحقّق منها على جهازك في TestFlight**،
وقائمتُها في §٨.

> **وأمانةٌ تُقال**: فحصٌ أوّلُ كان يسجّل دخولًا وينقر بطاقةً ثمّ يقطع الشبكة
> ليجرّب منعَ المزايدة. فسقط — لا لعطبٍ في الكود بل لأنّ النقرة لم تنتقل
> (الترطيب يستبدل العنصر) و`setOffline` لم تُطبَّق بعد تلك السلسلة. أي أنّه
> كان يقيس أداة الاختبار. فاستُبدل بما يقيس المقصود، وحارسُ المزايدة يُفحص
> في الوحدات.

## ٧. حدودٌ باقية

| الحدّ | لماذا بقي |
|---|---|
| **لا عملٌ دون اتّصال للمزادات** | **مقصود**: سعرٌ قديم يُبنى عليه قرارٌ أخطرُ من لا سعر. القرار مقيسٌ وموثَّقٌ في [`public/sw.js`](public/sw.js) |
| ~~لا ملاحةٌ سفلية~~ | **أُضيفت بعد هذا التقرير** — انظر §١٠ |
| الشارة تعتمد على فتح التطبيق | تصفيرُها من الخادم يحتاج إشعارًا صامتًا — عملٌ مستقلّ |
| `minVersion` لا يُلزِم | مقصود: تطبيقٌ يوقف صاحبه عن المزايدة لأنّ رقمًا تبدّل خطرٌ لا يُتحمَّل بلا قرار |
| `Package.swift` يَبلى محليًّا | `cap sync` يطلب Node ≥ 22 وجهاز التطوير على 20 — والبوّابة تُزامن وتتحقّق |

## ٨. ما يُجرَّب على جهازك — بالترتيب

1. **أعلى الشاشة**: الهيدر لا يلمس الساعة · الوقت والبطارية والشبكة ظاهرة
2. **صفحة مزاد**: زرّ رجوعٍ يمينًا · لا يظهر في السوق ولا الحساب
3. **حقل المزايدة**: اللوحة لا تغطّيه
4. **أطفئ الواي‌فاي**: شريطٌ أحمر يقول «لا اتّصال» · **والبطاقات باقية**
5. **اضغط «زايد» وأنت منقطع**: «تعذّر إرسال المزايدة لعدم وجود اتّصال» —
   ولا تُرسل بعدها
6. **أعِد الواي‌فاي**: «تم استعادة الاتّصال» · والسعر يُحدَّث
7. **إشعار** من صديقٍ يتجاوزك · واضغطه فيفتح المزاد
8. **اقرأ إشعاراتك** ثمّ انظر الأيقونة — الرقم يُمحى
9. **صغّر التطبيق دقيقتين** ثمّ عُد: السعر والمهلة محدَّثان
10. **أندرويد**: زرّ الرجوع يُغلق الورقة السفلية أوّلًا، ثمّ يرجع، ثمّ يُصغّر

## ١٠. ما أُضيف **بعد** هذا التقرير (٢٢–٢٤ سبتمبر)

> التقرير أعلاه يصف حالَ الغلاف يوم كُتب. وما تحته وقع بعده، ويُقرأ معه.

### الملاحة السفلية — والدُرج انسحب من الغلاف

كانت مستثناةً في الطلب ثمّ طُلبت. خمسةُ مواضع: الرئيسية · السوق · **زرٌّ مرفوع
في الوسط لإضافة لوحة** · لوحاتي · ملفّي. ورفعُ الأوسط ليس زينة: الإبهام يبلغ
وسط الشريط أسهل من طرفيه.

**ولا تُعرض في الويب** — والقرار في CSS لا في شجرة React: `data-native` تُوضع
على الجذر بعد الترطيب فيُخفى `[data-web-only]`، فلا شرطٌ يُحسب في كلّ مكوّن
ولا اختلافٌ بين ما رُسم على الخادم وما رُطِّب.

**وقبل إخفاء الدُرج نُقل ما فيه**: مفتاح إشعارات الجهاز ومفتاح الصوت وروابطه
صارت في صفحة الملفّ — وبلا هذا النقل يُقطع الطريق إلى الإشعارات بالكلّية.

### المستند لا يُمرَّر في الغلاف — تُمرَّر حاويةٌ داخله

وهذا أصلُ **ثلاثة أعطابٍ** ظهرت على الجهاز معًا: الهيدر يتزحزح عند التمرير،
والملاحة السفلية ترتفع فيظهر فراغٌ تحتها، وكلاهما «ثابت».

فصار `:root[data-native]` يقفل تمرير المستند، و`[data-app-scroll]` هي التي
تُمرَّر بـ`overscroll-behavior: none` — لا `contain`: `contain` تمنع تسريب
التمرير إلى ما فوقه ولا تمنع **ارتداد الحاوية نفسها**، والهيدر `sticky` داخلها
فما يرتدّ يرتدّ به.

### السحب للتحديث

مؤشّرٌ أسفل الهيدر يدور مع الإصبع بمقاومةٍ متزايدة. **وكان يموت بعد أوّل
تنقّل**: المستمعات على `[data-app-scroll]` المُلتقَط مرّةً عند التركيب، وموجِّه
Next يستبدل شجرة الصفحة — فتبقى على عقدةٍ مفصولة. فصارت على المستند في طور
الالتقاط، والحاوية تُطلب عند اللمس. ويُنهيه سحبٌ **عَرضيّ** فلا يزاحم
الكاروسيل.

### واجهة الرئيسية — ستوريز وبنرات

على الجوال والتطبيق وحدهما. التفصيل في [واجهة الرئيسية](docs/home-surface.md)
و[تخزين الوسائط](docs/media-storage.md).

### ما صار يُقاس

| الفحص | يحرس |
|---|---|
| [`native-shell.spec.ts`](tests/e2e/native-shell.spec.ts) | الملاحة في الغلاف وغيابُها في الويب · حاويةُ التمرير · **مفتاح الإعدادات داخل صفّه** |
| [`home-media.spec.ts`](tests/e2e/home-media.spec.ts) | الستوريز والبنرات والرفع والحدّ الخاصّ |

## ٩. خطوةٌ لاحقةٌ تخصّ الأصالة

**النشاط الحيّ** (Live Activities): عدّادُ مزادٍ على شاشة القفل والجزيرة
الديناميكية. وهو أقربُ ما يجعل التطبيق يسبق الأصيل لا يُنافسه، **وقناة APNs
المباشرة التي بُنيت أمسِ هي طريقُه** — لا يدعمه FCM.

وثمنُه ملحقُ واجهةٍ بـSwiftUI، وهو ما استُثني صراحةً. فيُعرض ولا يُنفَّذ.
