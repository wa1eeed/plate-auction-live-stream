# سجلّ التغييرات

كل التغييرات الملحوظة في هذا المشروع موثّقة هنا.

الصيغة مبنية على [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)،
والمشروع يتبع [الإصدار الدلالي](https://semver.org/lang/ar/).

التصنيفات: **أُضيف** · **تغيّر** · **أُهمل** · **حُذف** · **أُصلح** · **أمان**

---

## [غير منشور]

### أُضيف

**المحاسبة: أوامر الصرف**

- كل قرار بتحويل أو إعادة يفتح **أمر صرف** (`F26-00001`) في `/admin/payouts`:
  مستفيدٌ وآيبان ومبلغ وما خُصم عمولةً وضريبةً — والآيبان **لقطة وقت الإصدار**
  فلا يتحوّل المال إلى حسابٍ بُدّل بعد صدور الأمر.
- تنفيذه يشترط **مرجع حوالة** ويقيّد سحبًا مقابلًا في المحفظة: ما دخل بقيد يخرج
  بقيد، فلا يبقى رصيدٌ صُرف نظيره.
- إلغاؤه يُسقط الحوالة لا الاستحقاق — ومن آثر ترك ماله في المنصّة لا يُنقص منه شيء.
- **حساب الإيداع** في إعدادات المستخدم، والآيبان يُدقَّق بحساب mod-97 عند الحفظ
  لا عند فشل الحوالة. ومن لا حساب له يصدر أمره موقوفًا بلا زرّ صرف.

**الفوترة الإلكترونية (ZATCA) — مرحلة الإصدار**

- **فاتورة ضريبية مبسّطة** (`T26-00001`) عن كل عمولة: الوعاء الخاضع هو الوساطة
  لا ثمن اللوحة.
- **رمز QR** بترميز TLV/base64 بوسومه الخمسة، وطولُ كل وسم بالبايتات لا بالمحارف.
- **سلسلة تجزئة** تربط كل فاتورة بسابقتها، ولوحة الإدارة تعرض سلامتها مؤشّرًا.
- بيانات المنشأة تُنسخ في كل فاتورة، ولا تُصدَر فاتورة برقم ضريبي مختلّ ولا
  والفوترة معطّلة — الامتناع أسلم من إصدار باطل.
- صفحتا `/admin/invoices` و`/account/invoices`، وورقةٌ تُطبع لكلٍّ بحارسه.

**الضمان: المال يُحجز حتى تُنقل الملكية**

- السداد **يحجز ولا يُتمّ**. أربع مراحل جديدة: `escrow_held` ← `ownership_transferred`
  ← (`disputed`) ← `completed` أو `refunded`.
- البائع يرفع إثبات النقل، والمشتري يؤكّد فيُفرَج فورًا، أو يعترض فتفصل الإدارة،
  أو يسكت فيُفرَج تلقائيًا بعد **٧٢ ساعة** — قابلة للضبط من إعدادات الأدمن.
- **عمولة البائع تُخصم من عائده لا من محفظته**: كان اقتطاعها من المحفظة يُنتج
  «عمولة مستحقّة» على من تستحقّ له المنصّة مالًا.
- **عرابين الخاسرين تعود عند التحصيل** لا عند الاكتمال — المشتري أدّى فسقط
  احتمال إعادة الإرساء.
- الإفراج التلقائي هو الانتقال الوحيد بلا فعل بشري، فيسبقه تذكير قبل ٢٤ ساعة
  ويُقيَّد في التدقيق بلا منفّذ.
- **البائع لم يعد يملك «تمّت الصفقة»** (`USE_TRANSFER_FLOW`)، ولا الأدمن يُتمّها
  بتبديل حالة (`USE_RELEASE_FLOW`).

**أقسام المعاملات — بالدور لا بالمرحلة**

- «مشترياتي» و«مبيعاتي» و«صفقات الإدارة» صارت ثلاثة أقسام: **بانتظار ردّك**
  (وعند الإدارة **بانتظار قرارك**) · **تحت الإجراء** · **معاملة مكتملة**.
- والعدّاد ظاهر على كل قسم، فلا يُخفي التاب ما لا يُفتَح.
- **والبحث في جداول الإدارة يعلو على الأقسام**: يبحث فيها كلّها ويقول ذلك.
- **والمكتملة تُعرض مختصرة** بلا نداء ولا سكّة، فتقصر الصفحة إلى ثلثها.

**بيانات تجريبية لكل مراحل الصفقة**

- «مشترياتي» في حساب ماجد تعرض **المسار في حالاته السبع**: بانتظار السداد،
  ومهلة منقضية، والمبلغ محجوز، ونُقلت الملكية (بزرَّي التأكيد والاعتراض
  وعدّادهما)، واعتراض قيد المراجعة، واسترداد، ومكتملة.
- ومهلها **لم تنقضِ** عمدًا: مسح الضمان يجري على مسارات القراءة، فصفقةٌ مبذورة
  بمهلة منتهية تُفرَج عند أوّل فتح للصفحة فيرى صاحبها حالةً غير التي بُذرت.

**صفحة سداد موحّدة**

- `/checkout/{orderId}` — بريك داون كامل ووسيلة دفع، ثم `/thanks` بإيصال وزرّ
  «عرض الطلب». يصلها **الشراء المباشر** (تُسجَّل الصفقة أولًا فتُحجز اللوحة)
  و**الفائز بمزاد** من «مشترياتي» بزرّ «أكمل السداد».
- **رصيد المحفظة** وسيلة دفع ثالثة تُتمّ الصفقة فورًا، وإلى جانبها Tap والحوالة.
  والوسائل المعطّلة تُعرض بسبب تعطيلها لا تُخفى.
- `Payment.orderId` يفرّق شحن الرصيد عن سداد صفقة بعينها.

**مسار الصفقة وتفصيلها المالي**

- كل صفقة تعرض الطرح لا الحاصل: القيمة، ثم العربون، ثم العمولة وضريبتها، ثم
  **المطلوب سداده**.
- مسار من **خمس** مراحل ثابتة يتغيّر لونها لا عددها — فتُقرأ الحالة بلمحة.
- **نداء المرحلة الراهنة** يسبق المسار: ما المطلوب الآن، وممّن، وكم بقي من مهلته
  بعدّاد حيّ. والتسمية بمن عليه الدور لا بمن قصّر.
- **لا يظهر إلا زرّ صاحب الدور**: «أكّد نقل الملكية» للبائع في مرحلته، و«أكّد
  الاستلام» و«لديّ اعتراض» للمشتري في مرحلته.
- المسار نفسه في **ثلاثة مواضع**: «مشترياتي» و«مبيعاتي»، وصفحة الإعلان لطرفَي
  الصفقة وحدهما، و«حسابي» ضمن «يحتاج تصرّفك». وفي الإدارة طابور اعتراضات
  بقرارَين: إفراج للبائع أو استرداد للمشتري.
- **السكّة أفقية على كل المقاسات** بعد أن صار اسم المحطّة كلمةً («طلب» «سداد»
  «نقل» «تأكيد» «تم») لا جملة. والجملة باقية في نداء المرحلة و«تفاصيل المسار».
- **ثلاثة ألوان لا لونان**: أخضر لما قُطع، و**ذهبي لما يحترق الآن من مهلة
  المرحلة الجارية** فيُرى الزمن على الخطّ، ورمادي لما لم يبدأ.
- **السكّة تكبر بكبر المساحة**: من `lg` تغلظ ويكبر قطر محطّاتها ويظهر ختم كل
  محطّة تحت اسمها. الشريط الممدود على تسعمئة بكسل بمقاس الجوال يُقرأ «خطًّا
  عاديًّا» مهما أُتقن.
- **سطر «أين المال»** فوق السكّة بجملته كاملة — «المبلغ محجوز أمانةً لدى
  المنصّة» — ومَعلمٌ صامت يشير إلى محطّته. فسؤال الطرفين الأوّل في صفقة ضمان
  ليس «أين وصلنا» بل «أين مالي».

### أُصلح

**تدقيق تجربة وتصميم شامل — أربع عدسات على المنصّة كلّها**

- **صفحة الشكر كانت تكذب على من اختار الحوالة**: «استلمنا طلب سدادك — قيد
  المعالجة»، ولم يُستلم شيء؛ الحوالة تُنشأ `awaiting_transfer` والمشتري هو من
  يحوّل. فمن يقرأها يغلق الصفحة مطمئنًّا وتنقضي مهلته. صارت «بقي أن تحوّل
  المبلغ» ومعها بيانات الحساب وبلاغ رقم العملية.
- **الشراء المباشر من الجوال لم يكن يوصل صفحة السداد** — توست ثم يبقى المشتري
  مكانه وقد صار مرتبطًا بصفقة ومهلتها تجري.
- **زرّ «أرسل عرضك» على الجوال ميّت**: يشير إلى `#trade` ولا عنصر بهذا المعرّف
  في المستودع — أي أن طريقة بيع كاملة بلا زرّ عامل على الجوال.
- **شريط المزايدة على الجوال كان يزايد بلا إفصاح**: لا عربون ولا عمولة ولا
  رصيد؛ ومن لا يكفي رصيده لا يعرف إلا بعد الرفض.
- **صفحة السداد بلا مهلة**: مبلغٌ بعشرات الآلاف بلا تاريخ ولا عاقبة تأخير،
  بينما وعدت صفحة اللوحة بالمهلة صراحةً. صار فوقها نداء المرحلة بعدّاده،
  وتحت الزرّ عاقبة الانقضاء بقيمة العربون.
- **زرّان لا رجعة فيهما بلا سؤال**: «إلغاء» عند البائع، و«إتمام/إلغاء» عند
  الأدمن — صارا خلف حوار يذكر أثرهما بالعربون وبعمولة المنصّة.
- **زرّ «تمّت الصفقة» عند البائع** يرفضه الخادم منذ صار الإتمام إفراجًا — حُذف.
- **عدّادان مجمّدان يتظاهران بالحياة** في «نظرة عامة» و«مزايداتي»: `frozenMs`
  للمزاد المنتهي يُمرَّر لمزادات جارية، فيقرأ صاحبها رقمًا لا يتحرّك.
- **«إجمالي المبيعات» و«إجمالي مشترياتي»** كانا يجمعان ما لم يقع، والأوّل
  بالأخضر وبقيمة قبل عمولتنا. صارا أرقامًا بأسمائها من `settlement.net`.
- **بحث الأدمن بالرقم المرجعي الكامل كان يُخفي الجدول كلّه** في صفحتَي
  المدفوعات والتدقيق — لأنهما لا تمرّران `reference` فتسقط كل الصفوف.
- **السوق كان يقول «عرض 12 من 47»** بلا رقاقة ولا تفسير: فلتر «المتاح للتداول»
  الافتراضي يُخفي 35 لوحة ولا يُعدّ لأنه الافتراضي.
- **منع التكبير باليدين** (`maximumScale: 1`) — يمنع قراءة الآيبان ورقم اللوحة.
- **حلقة التركيز مبتلعة** في بطاقة السوق، فالتنقّل بلوحة المفاتيح أعمى.
- **الذهبي الصغير دون حدّ التباين**: `gold-500` على أبيض = 3.65:1 والحدّ 4.5.
  النصّ الصغير صار `gold-400` (5.37:1)، والقاعدة في نظام التصميم §5.4.
- **«كيف يعمل السوق» يَعِد بخطوتين لا وجود لهما** — «علّم الصفقة تمّت»
  و«بيانات التواصل».
- **أمان: نموذج الدخول كان يرتدّ إلى GET** عند تعطّل الحزمة، فتُكتب كلمة
  المرور في شريط العنوان وسجلّ التصفّح وترويسة `Referer`. رُصد حيًّا أثناء
  الالتقاط: `‎/login?email=…&password=…`. صار `method="post"` — ومثله نموذج
  إعدادات الدفع الذي يحمل مفتاح البوابة.
- **جداول الإدارة السبعة وكشف الحساب كانت تمرّر أفقيًّا** ثلاثة أضعاف عرض
  الجوال، وعمود الإجراءات آخرها — فيُدفَن ما يُفعل خلف الحافّة. صارت تنقلب
  **بطاقات** تحت `sm`، كل خليّة باسم عمودها؛ والاسم يصل من تعريف الأعمدة نفسه
  عبر `--col-N` فلا يُكتب مرّتين ولا تُعدَّل سبع صفحات.
- **«مزايداتي» كانت صفًّا واحدًا** مرتّبًا بمن هو الأعلى: مزادٌ لم يبقَ له
  دقيقتان تحت مزادٍ أمامه ثلاثة أيام. صارت ثلاثة أقسام مرتّبة بالإلحاح.
- **نموذج إضافة اللوحة صامت**: لا قاعدة ولا رسالة خطأ — من يترك رقم اللوحة
  فارغًا يضغط «حفظ» فلا يحدث شيء ظاهر.
- **البائع لم يكن يرى عمولتنا وهو يسعّر**، ولا يرى قواعد المنصّة إلا في المزاد.
- **سجلّ التدقيق كان يطبع قيمًا خامًّا**: `status: held` بالإنجليزية،
  و`amount: 250000` لصفقة قدرها 2,500 ريال.
- **إعلان أوقفته الإدارة كان يبدو كإعلان بيع بنجاح** — شارة واحدة لأربع حالات.
- **العمولة المستحقّة لا تظهر إلا في المحفظة** وهي دَينٌ على صاحبه.
- **القسم المفتوح لا يعيش في الرابط** فتحديث الصفحة يعيدك إلى الافتراضي.
- **ما يفيض عن حافّة الشاشة كان يُقصّ صامتًا** في التنقّل والتابات — صار يتلاشى.
- **الأزرار المصغّرة تحت أرضية اللمس** — وُسّعت منطقتها بلا تكبير شكلها.
- **«ازفر بمزاد»** تصحيفٌ لـ«اظفَر».
- **وضع العرض التجريبي كان يعمل بالافتراض**، فيُنشر بكلمة مرور عاملة ما لم
  يُطفأ صراحةً. صار يُطلب طلبًا (`DEMO_MODE=true`).


- **البائع كان يقرأ في «مبيعاتي» صفحةً مكتوبة للمشتري.** `buildOrderTimeline`
  لم تكن تعرف لمن تتحدّث، فتُبنى بصوت المشتري وتُعرض للطرفين: «اشتريت اللوحة»،
  «وصل **مبلغك** وحُجز أمانةً»، «**نقل البائع** الملكية» — يُحدَّث البائع عن
  نفسه بضمير الغائب، ويُطلب منه أن «يراجع الإثبات» الذي رفعه بنفسه. صارت
  `side` **إلزامية** بلا افتراض صامت، فالافتراض الصامت هو ما أنتج العيب.
- **بطاقة البائع كانت تجمع عمولتنا إلى قيمة صفقته وتسمّيها «المطلوب سداده»** —
  تقول لمن يقبض إنه يدفع، وبرقم أكبر من صفقته: لوحة بـ9,000 وعمولة 2.5٪ تُعرض
  «9,258.75» والصواب أن يصله **8,741.25**. صار الحقل `net` باتجاهين مع `side`،
  والعمولة تُطرح ممّن يقبض وتُضاف على من يدفع.
- **صفقة أُفرجت بقرار إدارة بعد اعتراض** كانت تقول «أذن المشتري بالإفراج أو
  انقضت مهلته» — كذبٌ يمحو خصومةً وقعت. صارت تقول «فُصل الاعتراض».
- **انقضاء مهلة نقل الملكية لم يكن يظهر في المسار** رغم أن الواجهة تعرض عندها
  زرّ «اطلب الاسترداد»: صار يُعلَن، ولكلٍّ ما يملكه عنده.
- **«اعتراضك» و«اعتراض الطرف الآخر»** كانا نصًّا واحدًا، والفرق يغيّر الموقف.
- **«يحتاج تصرّفك» كان يسوق البائع إلى «مشترياتي»** في اعتراض على بيعه.
- **صفحة «مبيعاتي» كانت تطلب من البائع أن يعلّم الصفقة «تمّت»** — زرٌّ لم يعد
  موجودًا منذ صار الإتمام إفراجًا.

- **أمان: البائع كان يُتمّ صفقة لم تُدفع** فيقبض عربون المشتري ويُغلق البيع بلا
  ثمن. أُكّد حيًّا: صفقة بـ38,000 غير مدفوعة عُلّمت مكتملة بـ`200`. صار الإتمام
  يشترط دفعة مختومة (`ORDER_NOT_PAID`) — للبائع وللأدمن معًا.
- **أمان: `setOrderStatusByAdmin`** كانت تقبل أي حالة من أي حالة بلا فحص تمويل،
  وتسمح بالخروج من `completed` بعد أن خرج المال. أُغلقت.
- **حارس السداد صار قائمة سماح**: كانت `defaulted` تمرّ فتُسدَّد صفقة أُعيد
  إرساؤها على غير مشتريها.
- **إعادة الإرساء ترفض صفقة وصل مالها** (`ORDER_ALREADY_PAID`) — كانت تتركه
  يتيمًا بلا صفقة تقابله.
- **ردّ الدفعة العالقة**: قيد `purchase_refund` يُعيد ما حُصّل عن صفقة لم تُسوَّ
  إلى رصيد صاحبها — لم يكن في المستودع مسار ردّ إطلاقًا.
- **السداد بالبطاقة لم يكن يصل Tap إطلاقًا**: تُنشأ عملية وتُعاد `redirectUrl: null`
  بلا `createCharge`، فيصل المشتري صفحة الشكر ولم يدفع. صار يُنشئ شحنة فعلية
  ويعود إليها فتُزامن نتيجتها قبل بناء الصفحة.
- **تأكيد حوالة صفقة كان يشحن المحفظة**: `markPaymentPaid` تكتب `topup` دائمًا
  ولا تقرأ `orderId`، فيجد من حوّل ثمن لوحة المبلغ رصيدًا في محفظته وتبقى صفقته
  «بانتظار السداد» أبدًا.
- **العمولة كانت تُعرض ولا تُحتسب** في المطلوب سداده، فيرى المشتري رقمًا أقلّ
  ممّا سيدفع.
- **خصم مزدوج عند السداد من المحفظة**: خرج 37,012.50 بدل 32,006.25 لأن العمولة
  حُسبت مرّة ضمن المطلوب ومرّة في `chargeOrderCommission`.
- **مفتاح مصادرة العربون داخل `<label>`**: الضغط على النصّ الشارح كان يُمرَّر
  إليه فيُطفئ قرار مصادرة مالٍ بلا أن يشعر المنفّذ. وصار تنبيه إعادة الإرساء
  يقول ما وقع بالمال: «وصودر عربون فلان» أو «بلا مصادرة».
- **إعادة الإرساء كانت تقع مرّتين** فتُنشئ صفقتين متطابقتين على اللوحة نفسها —
  `ALREADY_REAWARDED` يمنعها، ودليلها رسوّ الإعلان لا ترتيب التواريخ.
- **إعادة الإرساء بلا مصادرة** كانت تُبقي عربون المتخلّف محجوزًا على مزادٍ خرج
  منه؛ صار يعود إلى رصيده.

### تغيّر

**الإفراج صار قرار إدارة**

- بعد رفع البائع إثبات النقل **تتحقّق الإدارة ثم تُفرج**. والمشتري **لا
  يُطالَب بشيء**: لا زرّ تأكيد، ولا مالٌ يخرج بسكوته.
- **حُذف الإفراج التلقائي** ومعه مسار `POST /api/orders/{id}/confirm`. وبقاؤه
  كان يعني خروج مالٍ بلا قرارٍ من أحد — نقضًا للنموذج نفسه.
- **وباب السؤال لا يُغلق**: لكلا الطرفين رفع استفسار في أي مرحلة حتى الإفراج.
  وأثره بموضع المال: محجوزًا يُجمَّد الإفراج حتى تفصل الإدارة، وقبل السداد
  يُسجَّل ويُرفع **بلا تجميد** — فتجميد صفقةٍ لم يصل مالها يمنع صاحبها من
  الدفع، أي يُعاقَب على سؤاله.
- **`escrowAutoReleaseHours` صارت `escrowReviewWindowHours`**: اسمٌ يصف ما تفعله
  فعلًا — مهلةٌ تُقاس بها تأخّر الإدارة، وتُذكَّر بها ولا يتحرّك بها مال.
- **ومهل الضمان صارت تُضبط من اللوحة** (نقل الملكية · مراجعة الإدارة) بعد أن
  كانت ثابتة في الكود بينما قيل إنها قابلة للضبط.

**صفقات الإدارة بطاقات ممتدّة لا جدولًا**

- الصفقة تحمل **مهمّة بجملتين** وطرفين ومبلغًا وثلاث مهل — وأحد عشر عمودًا
  لذلك تعني سبعين ريمًا من العرض، وتمريرًا أفقيًّا يدفن عمود الإجراءات في
  آخره. صارت **بطاقة ممتدّة**: هويّة الصفقة ومالها في صفّ، والمهمّة والمهل
  والأزرار في صفّ تحته — والأزرار في طرف البطاقة حيث تُطلَب.
- ولا تمرير أفقي في أي عرض: **صفر فائض** على 1400px وعلى 390px معًا.

**بطاقات السوق صفٌّ ممتدّ على الجوال**

- البطاقة العمودية تعطي على شاشة ضيّقة لوحةً بعرض الشاشة، فلا يظهر في الطيّة
  إلا واحدة ونصف. والصفّ يُظهر ثلاثًا أو أربعًا: ارتفاع البطاقة نزل من ~340px
  إلى **162px** للمزاد و**130px** للبيع المباشر.
- **واللوحة نصف البطاقة** — كبرت من ~130px إلى **158px** فصارت تُقرأ لا تُلمح.
- **والوسمان نزلا تحت اللوحة** إلى الفراغ الذي كان مهدورًا حولها: طريقة البيع
  ونوع اللوحة، **بتصميم أخفّ** بلا حدٍّ ولا أيقونة — نقطةٌ ملوّنة ونصٌّ صغير.
  الحدّ والأيقونة يصنعان كتلةً تزاحم اللوحة، والوسم يصف ولا يُنادي. والرقم
  المرجعي نزل إلى التذييل مع البائع.
- **وعدّاد بخانات**: أربع خانات مربّعة (يوم · ساعة · دقيقة · **ثانية**) بأرقام
  جدولية وتسمية تحت كلٍّ، بلا كلمات موصولة («يومان و23 س»). عرضه لا يتغيّر
  بتغيّر القيمة فلا يرقص الصفّ كل ثانية، ويحمرّ نابضًا في الدقائق الأخيرة.
  والثانية هي ما يجعله **حيًّا** لا صورةً لوقتٍ مضى. وكتلته الكبيرة تبقى من `sm`.
- **واسم البائع خرج من البطاقة**، ونزلت مكانه **الدعوة** («التفاصيل والمزايدة»)
  سطرًا كامل العرض: اسم البائع لا يُقرَّر به شراء لوحة، وسطرٌ بعرض البطاقة
  أسهل إصابةً بالإبهام من سطرٍ محشور بين السعر وحافّة العمود.

**جدول صفقات الإدارة يقول القرار لا الحالة**

- عمود **«المطلوب الآن»** في كل صفّ: «افصل في اعتراض المشتري» بسببه، «تحقّق من
  نقل الملكية ثم أفرج» بإثبات البائع، «انقضت مهلة السداد — صادر أو ألغِ»،
  «تأخّر البائع عن نقل الملكية». وقسمٌ اسمه «بانتظار قرارك» يجمع ثلاثة قرارات
  مختلفة بلا تمييز يزيد التشتّت لا يرفعه.
- **وأزرار الإفراج/الاسترداد** تظهر على ما نُقلت ملكيته كما تظهر على المعترَض
  عليه، ونصّ الحوار يعرض **إثبات النقل** ليُقرأ قبل القرار لا بعده.


**لمسات تصميم**

- **السمة الفاتحة صعدت إلى جذر المستند**: كانت على عنصرٍ داخل `body`، والنوافذ
  المنبثقة (الإشعارات، دُرج الجوال، القوائم) تُصيَّر في `body` **خارجه** عبر
  Portal — فترث الرموز الداكنة وتظهر سوداء في منصّة فاتحة. وحجاب الدُّرج اسودّ
  ثابتًا: `ink-950` هو خلفية الصفحة لا نقيضها، فحجابٌ منه وشاحٌ أبيض على أبيض.
- **«مزاد مباشر» بدل «متصل لحظيًا»**، بأيقونة تنبض بحلقة تتمدّد وتتلاشى:
  الحالة تقنيّة والزائر لا يعنيه المقبس — يعنيه أن ما يراه يحدث الآن.
- **عدّاد صفحة الإعلان يصغر على الجوال** فيظهر كاملًا: أربع خانات بعرض
  `3.25rem` وفواصلها كانت تتجاوز عرض العمود على 390px.
- **واللوحة على منصّة عرض**: سطح متدرّج بتوهّج ذهبي خافت وظلٍّ تحتها — كما
  تُعرض قطعة في صالة. وسُطِّح التداخل: كان العدّاد والمزايدة التالية صندوقين
  داخل صندوق السعر، فصارا مفصولين بخطٍّ شعري.
- **تصفّح السوق على دفعات**: اثنتا عشرة بطاقة ثم تنمو عند بلوغ حارس الأسفل
  (`IntersectionObserver` بهامش 600px فلا يُرى انتظار)، ومعها زرّ «عرض المزيد»
  صريح — التمرير اللانهائي وحده يحبس من يتنقّل بلوحة المفاتيح. **وبلا طلب
  شبكة واحد**: التصفية والترتيب في المتصفّح، فالحِمل على الخادم أقلّ ما يكون —
  طلبٌ واحد لا طلب لكل صفحة. وحين يكبر المعروض إلى ألوف تصير التجزئة على
  الخادم لازمة، وموضعها `/api/listings`.

- **اللوحة صارت جسمًا لا رسمًا**: حروفها **منقوشة** كما تُطرَق الحقيقية (ظلٌّ
  أبيض إلى أعلى وداكن إلى أسفل بلا تشويش)، ووجهها بتدرّج معدني خافت. والنقش
  للمقاسات الكبيرة وحدها — مرشّحٌ لكل بطاقة في شبكة من أربعين كلفةٌ بلا مقابل.
- **لمعةٌ تمرّ على اللوحة** عند تحويم بطاقة السوق: السطح المعدني يُعرَف
  بانعكاسه، وبطاقةٌ ساكنة تمامًا تبدو صورةً لا شيئًا يُمسك.
- **لوحة الإدارة صارت لوحة قيادة**: صدرها «أمانةٌ في يد المنصّة الآن» بشريط
  مقسوم (عرابين محجوزة · مبالغ صفقات محبوسة) — وهو أهمّ رقم فيها وكان مبعثرًا
  بين ثلاث صفحات؛ ومعه **منحنى إيراد سبعة أيام** بلا مكتبة رسم، محورُه من
  اليمين إلى اليسار فيمشي مع القراءة.
- **لحظة اكتمال الصفقة**: «صارت لك» للمشتري و«وصل عائدك» للبائع، باللوحة
  والمبلغ والرقم المرجعي — تُعلَن **مرّة واحدة** ثم تُطوى، فالاحتفاء المتكرّر
  ضجيج. وذاكرتها في المتصفّح لا في قاعدة البيانات: زينةٌ لا تستحقّ حقلًا.

- **هياكل انتظار** لصفحات الحساب والسوق وصفحة الإعلان: الصفحات `force-dynamic`،
  فكان الانتقال يترك الشاشة على السابقة حتى يردّ الخادم — يبدو الضغط بلا أثر
  فيُعاد. والهيكل يحجز مواضع المحتوى فلا تقفز الصفحة عند وصوله. ولمعانه
  `background-position` لا `opacity`، فيبقى مرئيًّا بلا سكربت.
  - ولوحة الإدارة **بلا هيكل عمدًا**: ثبت أنه يُعلّق التنقّل داخل اللوحة ستّين
    ثانية، وهي أداة تشغيل على الحاسوب لا يزيدها الهيكل شيئًا.
- **وزن العناوين العربية** (`text-wrap: balance`) فلا تبقى كلمة يتيمة في سطر —
  وهو أظهر في العربية حيث تطول الكلمة؛ و`pretty` للفقرات.
- **تصريف المعدود بالعربية**: كانت الواجهة تكتب «3 صفقة» في سطر و«2 لوحات»
  في سطر آخر من الملفّ نفسه. صارت `arabicCount` تصرّف بستّ صيغ من
  `Intl.PluralRules('ar')` — والمفرد والمثنّى يُغنيان عن الرقم.
- **الأسهم تنقل بين التابات** وتبويبةٌ واحدة تدخل الشريط وتخرج منه
  (roving tabindex): `role="tab"` عقدٌ لا وسم، وكان الشريط يعلنه ولا يفي به.
  والاتجاه يُقرأ من `dir` المحسوب لا من افتراض في الكود.
- **حُذف `count-flash`** — تعريف بلا استعمال، والسعر يتحرّك بـFramer أصلًا.


- **التواريخ رقمية بالشرطة المائلة** في المنصّة كلّها — `04/09/2026 · 11:55` —
  بلا اسم شهر وبلا اختصار «اليوم/أمس»: اسم الشهر يهزّ أعمدة الجداول، والاختصار
  يمنع مقارنة الأسطر بعضها ببعض.
- **«مُفكّ» صارت «متاح في المحفظة»**: صاحب المال يسأل أين صار لا ما فُعل به.
- **نصوص «السداد خارج المنصّة» أُزيلت** من «مشترياتي» و«كيف تعمل المنصّة» ونموذج
  الإعلان وصفقات الإدارة وبطاقة التسوية وإعدادات العمولة: صارت تصف واقعًا لم يعد
  قائمًا بعد الضمان — المال يمرّ بالمنصّة ويُحجز فيها.

**نظام إشعارات لحظي**

- ثلاثة عشر نوعًا من الإشعارات، وأهمّها **«تجاوزك مزايد»**: مزاد بلا هذا التنبيه
  يخسر فيه المزايد لوحته وهو لا يدري أنه خرج من السباق.
- **يستعمل اتصال WebSocket نفسه** الذي يحمل المزايدات — لا اتصال ثانٍ ولا
  استطلاع، وموضوع مستقلّ لكل مستخدم `user:{id}`.
- جرس في الترويسة بعدّاد غير المقروء، يُعلّمها مقروءة عند فتح القائمة، ويستدعي
  إنعاش الصفحة لأن الإشعار غالبًا يعني تغيّرًا فيها.
- **الإشعار لا يُسقط العملية**: `notify` لا ترمي أبدًا — فشل التنبيه يجب ألّا
  يُسقط المزايدة التي أنشأته.
- من **لا** يُنبَّه محسوب بدقّة: المزايد بتجاوز أحدثه هو، والبائع في مزاده،
  والفائز برسالة الخسارة.

**لوحة تحكّم المستخدم**

- أُعيد بناء `/account` على مبدأ **ما يحتاج تصرّفًا أولًا**: صفقة تجاوزت مهلتها،
  مزاد تجاوزك فيه غيرك، عرض ينتظر ردّك، مسودّة لم تُنشر — قبل الأرقام المجرّدة.
- بطاقة المحفظة في المقدّمة: الرصيد المتاح والمحجوز — لا مزايدة بعربون بدونه.
- «مزادات أنت فيها» مرتّبة بالأقرب انتهاءً بعدّاد لكلٍّ منها، و«آخر التنبيهات».

**لوحة الإدارة**

- **صفحة سجلّ التدقيق**: كنّا نكتب `AuditLog` منذ البداية بلا واجهة تعرضه.
  الآن يظهر بالفعل ومنفّذه وحالته قبل وبعد، والأفعال التي تمسّ المال مُبرَزة.
- **بحث فوري في كل الجداول** الإدارية (المستخدمون، الإعلانات، الصفقات،
  المدفوعات، العرابين، الحركات، التدقيق) — يخفي الصفوف بلا إعادة بناء الجدول.


**نظام تصميم وإعادة تصميم كاملة**

- رموز تصميم موحّدة: منحنيات حركة (`--ease-spring` / `--ease-smooth`)، ومُدد،
  وسُلّم ارتفاع **بتعريفين** — الظلال لا تُرى في السمة الداكنة فالارتفاع فيها
  بالحدود والإضاءة، ويُرى في الفاتحة فيحمله الظلّ.
- أدوات مساعدة: `surface` · `glow` · `ring-gold` · `dot-grid` ·
  `scrollbar-none` · `edge-fade-start`.
- بدائيّتان جديدتان على Radix: **`Sheet`** (دُرج) و**`DropdownMenu`**، بحركات
  معرَّفة عندنا مربوطة بـ`data-state` بدل إضافة خارجية.
- **صفحة رئيسية جديدة**: بطل تتوسّطه لوحة حقيقية من السوق وخلفها لوحتان
  مائلتان، وثلاث خلاصات بكاروسيل لكل طريقة بيع (مزاد ذهبي · بيع مباشر أخضر ·
  سوم سماوي) بلون يميّزها بلمحة.
- **كاروسيل** على تمرير أصلي مع `scroll-snap`: السحب باللمس ولوحة المفاتيح
  يعملان بلا كود، وقارئ الشاشة يجد قائمة عادية. الأسهم إضافة لمستخدمي الفأرة
  وتُعطَّل عند الحافة، و`scrollLeft` يُقاس بقيمته المطلقة لأنه سالب في RTL.
- **ترويسة جديدة**: مؤشّر منزلق بين الروابط (`layoutId`)، وقائمة حساب تعرض
  الرصيد المتاح، ودُرج جوال يُغلق تلقائيًا عند الانتقال.
- **تذييل** لم يكن موجودًا: روابط الأقسام والحساب، وثلاث ضمانات، والتنويه
  القانوني في كل صفحة لا في الرئيسية وحدها.
- **فلاتر جديدة**: شرائح مرئية لطريقة البيع بمؤشّر منزلق، ودُرج للفلاتر الأقلّ
  استعمالًا بعدّاد على زرّه، ورقائق قابلة للإزالة لما هو مفعّل.
- بيانات بذرة أوسع: 16 لوحة موزّعة على طرق البيع الثلاث بمدد وأسعار متنوّعة.

**حوكمة موحّدة للمزادات**

- **قواعد المزاد انتقلت من البائع إلى الإدارة**: العربون (نسبة بحدّين أو مبلغ
  ثابت) ومهلة السداد والتمديد التلقائي تُضبط مرّة واحدة في
  `/admin/settings` ← «قواعد المزاد»، مع معاينة حيّة للعربون عند 10 آلاف و50
  ألفًا و250 ألفًا. العربون شرط دخول لا ميزة إعلان: بائع يرفعه يُقصي المزايدين،
  وبائع يُصفّره يفتح مزاده للعابثين.
- نموذج الإدراج **يعرض** هذه القواعد ولا يُتيح تحرير حقل منها — معرفةً لا تصرّفًا.
  وتغيير الإعدادات لا يمسّ الجاري: كل إعلان يحمل نسخته وقت نشره.
- **إعادة الإرساء على المزايد التالي** عند تخلّف الفائز: قائمة المزايدين
  الباقين مرتّبة تنازليًا بحالة عربون كلٍّ منهم، **ومصادرة عربون المتخلّف خيار
  داخل الخطوة نفسها** لا إجراء منفصل — فهما قرار واحد. الصفقة الجديدة بمهلة
  سداد كاملة، وسبب إلزامي يُقيَّد في سجلّ التدقيق.

**صفحة المستخدم في الإدارة — نظرة 360°**

- بطاقات المحفظة ومؤشّرات النشاط ووسوم الحالة في الأعلى، ثم تبويبات: النشاط
  (الإشعارات والمزايدات)، المال (كشف الحساب والعرابين والمدفوعات)، لوحاته،
  وصفقاته بيعًا وشراءً — بدل قوائم متتابعة تُقرأ بالتمرير الطويل.

**أرقام مرجعية موحّدة — `U26-00001`**

- **رقم مقروء لسبعة كيانات**: المستخدم `U` · الإعلان `L` · الصفقة `S` ·
  الدفعة `P` · العربون `D` · حركة المحفظة `W` · قيد إيراد المنصّة `R`.
  المعرّف الداخلي `lst_9f2c…` لا يُملى في مكالمة ولا يُكتب في رسالة دعم —
  والمنصّة متعدّدة المستخدمين، فيحتاج كل طرف رقمًا يُعرّف به معاملته.
- **البنية**: حرف النوع + السنة الميلادية بخانتين + تسلسل من خمس خانات.
  والتسلسل **يبدأ من جديد كل سنة**، وهو ما يجعل السنة ذات معنى بدل أن تكون
  زينة: `L27-00001` أول إعلان في 2027 لا الإعلان رقم 12,346.
- **`O` و`I` ممنوعتان في البادئات**: تُقرآن صفرًا وواحدًا عند الإملاء، فرقمٌ
  يُملى بهما يُدخَل خطأً. لذلك «صفقة» حرفها `S` لا `O`.
- **الشرطة موجودة والمحلّل يقبل الصيغتين**: ثمانية محارف متّصلة تُقرأ بخطأ
  أكثر من تسعة مفصولة، ومن كتبها بلا شرطة وجد ما يبحث عنه.
- **البحث يطابق تامًّا لا جزئيًا**: `L26-1` لا تسرد `L26-00010` ولا
  `L26-00011`. ويقبل الأرقام العربية والحروف بأي حالة.
- **أرقام اللوحة تبقى أرقام لوحة**: `4040` بلا حرف نوع تبحث في اللوحات لا في
  المراجع.
- **لا يُعاد استعمال رقم**: العدّاد يزيد ولا ينقص، ومحاولة فاشلة لا تستهلك
  رقمًا — رقمٌ في رسالة قديمة يجب ألّا يشير يومًا إلى شيء آخر.
- **رقم العضوية لا يغادر الخادم** في الحمولات العامة: `seller` في تفاصيل
  الإعلان اسمٌ ومدينةٌ وتاريخ عضوية فقط.
- مرجع عمليات الشحن هاجر من `TOP-A3F9K2` العشوائي إلى `P26-00019` الموحّد.
- **رابط صفحة المستخدم في الإدارة صار يحمل رقم العضوية** (`/admin/users/U26-00003`)
  لا المعرّف الداخلي، فيتطابق شريط العنوان مع ما في الصفحة ومع ما يراه المستخدم
  في حسابه. والمعرّف الداخلي يبقى مقبولًا فلا تنكسر روابط محفوظة.
- رقم الحساب ظاهر للمستخدم في **«حسابي» وفي الإعدادات** بشارة قابلة للنسخ،
  ومطابق حرفًا بحرف لما تراه الإدارة في ملفّه.
- سُمّي «رقم الإعلان» لا «رقم اللوحة» عمدًا: للوحة أرقامها المطبوعة عليها،
  وخلطهما يجعل «اللوحة 4040» جملة غامضة.

**عمولة المنصّة وضريبتها وحساب الإيرادات**

- **عمولة على كل صفقة تكتمل**، لكل طرف إعداده المستقلّ: البائع أو المشتري أو
  كليهما، نسبةً من قيمة الصفقة أو مبلغًا ثابتًا، بحدّ أدنى وسقف. معطّلة
  ابتداءً — لا تُقتطع من أحد بلا قرار واعٍ.
- **ضريبة القيمة المضافة على العمولة وحدها** لا على قيمة اللوحة: المنصّة تبيع
  وساطة لا تبيع اللوحة، فوعاء الضريبة هو أجر الوساطة.
- **تُقتطع من المحفظة لا من ثمن اللوحة** — الثمن لا يمرّ بالمنصّة أصلًا.
  ومن لا يكفي رصيده تُقيَّد عمولته **مستحقّة** وتظهر له في محفظته وللإدارة
  للتحصيل: تعليق صفقة أُتمَّ سدادها خارج المنصّة عقوبةٌ على الطرفين لأجل دَينٍ
  يخصّ أحدهما.
- **إفصاح قبل الالتزام**: تظهر في لوحة التداول وبيانات الإعلان محسوبةً على
  السعر القائم، فيعرف المزايد ما سيدفعه لو رست عليه الآن. رسمٌ يُكتشف بعد
  الالتزام يُفسد الثقة أكثر ممّا يجمع من إيراد.
- **`/admin/revenue`**: كل ريال دخل المنصّة أو استحقّ لها — عمولات وضرائب
  وعرابين مُصادَرة — بحالة كلٍّ: محصَّل · مستحقّ · مُبطَل.
- كل ريال يخرج من محفظة مستخدم يدخل **قيدًا مقابلًا** في حساب المنصّة، فصار
  الدفتر مزدوجًا بدل خصم من طرف واحد يتبخّر أثره.

**تذكيرات مهلة السداد**

- تذكير قبل انقضائها بـ24 ثم بـ6 ساعات، وإعلانٌ عند انقضائها يصل المشتري
  **والبائع**. المصادرة بعد إنذار لا مفاجأة.
- `remindersSent` على الصفقة يمنع التكرار، والمسح يجري على مسارات القراءة لا
  في مؤقّت خلفي لا ينجو من إعادة تشغيل.

**لمسات على المؤشّرات**

- أيقونة لكل مؤشّر، و**شريط نسبة** حيث تكون النسبة هي المعنى (العرابين المحجوزة
  كنسبة من أرصدة المحافظ)، و**وسم نابض** على ما ينتظر تصرّفًا فعلًا، وشريط لوني
  يسبق عنوان كل مجموعة فلا تُقرأ الثلاث كقائمة واحدة.

**فلاتر وأصوات وأوقات**

- **فلترة بعدد الحروف** (أحادي/ثنائي/ثلاثي) **وعدد الأرقام** (1–4): ليست أوصافًا
  بل فئات سعرية معروفة في سوق اللوحات.
- **أصوات مولّدة بـWeb Audio** لا ملفات: مزايدة، تجاوز، فوز، تكّة العدّ الأخير،
  تنبيه، نجاح. صامتة افتراضيًا، والتفضيل محفوظ محليًا.
- **كشف المزايدات بختم زمني كامل بتوقيت جهاز القارئ** أيًّا كان: يُصيَّر أولًا
  بتوقيت الرياض فيقرؤه من لا جافاسكربت لديه، ثم يتحوّل بعد التركيب إلى توقيت
  الجهاز، و`<time dateTime>` يحمل القيمة المطلقة للأدوات.

### تغيّر

- **نصّ البطل باللهجة السعودية**: «لوحتك تسوى أكثر — بِعها بسعرها الصح»،
  ونصّ مباشر بلا تكلّف («سعرك الاحتياطي ما يشوفه أحد غيرك»)، وشارة «مزادات
  شغّالة الحين»، ونداء فعل «شوف اللوحات».


**بوابة الدفع Tap وشحن المحفظة**

- تكامل كامل مع [Tap](https://developers.tap.company): إنشاء عملية دفع، وصفحة
  دفع مستضافة (`src_all`) فلا تمرّ بيانات البطاقات بخوادمنا إطلاقًا، وقراءة
  الحالة من البوابة بوصفها مصدر الحقيقة الوحيد.
- **ويبهوك بتوقيع إلزامي**: HMAC-SHA256 على حقول Tap بترتيبها المحدّد، ومقارنة
  ثابتة الزمن. بلا هذا التحقّق يستطيع أي طرف إرسال «تم الدفع» فيشحن رصيده مجانًا.
- **تبديل البيئة التجريبية والحقيقية** من لوحة الإدارة، مع تحذير صريح عند
  تفعيل الحقيقية، وشارة «وضع تجريبي» للمستخدم في الوضع التجريبي.
- **المفاتيح السرّية من متغيّرات البيئة وحدها** — لا تُخزَّن في قاعدة البيانات
  ولا تُعرض ولا تُدخَل في أي حقل. الأدمن يختار البيئة لا المفتاح، ولا يمكن
  تفعيل بيئة بلا مفتاح.

**الحوالة البنكية**

- يفعّلها الأدمن ويضبط بيانات الحساب (البنك، اسم الحساب، الآيبان، رقم الحساب،
  تعليمات إضافية) — ولا يظهر الخيار بلا آيبان.
- المستخدم يرى البيانات مع **زرّ نسخ لكل حقل** ومرجعًا فريدًا `TOP-XXXXXX`
  يكتبه في ملاحظات التحويل، ثم يُرفق رقم عمليته.
- صفحة `/admin/payments` بحالة قابلة للتأكيد أو الرفض بعد مطابقة الحوالة،
  والقرار ينعكس فورًا على حالة العملية عند المستخدم ورصيده.
- **منع الشحن المزدوج**: كل عملية تحمل `ledgerEntryId`؛ وجوده يعني أن الرصيد
  أُضيف، فويبهوك مكرّر أو تحديث صفحة أو تأكيد إداري ثانٍ لا يفعل شيئًا.

**عدّاد تنازلي جديد**

- كتل رقمية منفصلة (يوم · ساعة · دقيقة · ثانية) بأرقام جدولية وعرض ثابت،
  فلا يرتجف التخطيط ستّين مرة في الدقيقة.
- **إلحاح متدرّج** لا ثابت: عادي ← يقترب الحسم (أقل من ساعة) ← الدقائق الأخيرة
  (أقل من 5) ← الثواني الأخيرة (أقل من دقيقة)، يشتدّ فيها اللون وتنبض الثواني
  وحدها لا كل الكتل.
- **شريط ما مضى** من مدّة المزاد — يعطي المزايد سياقًا لا رقمًا مجرّدًا.
- **تفاعل مع التمديد اللحظي**: يومض ويعلن «مُدّد المزاد» عند وصول
  `time_extended` عبر WebSocket، فلا يظنّ المزايد أن العدّاد اختلّ.
- **عدّاد البطاقات صار حيًّا**: كان جامدًا على لحظة الجلب، والآن يتحرّك بمرجع
  وقت الخادم الذي صار يرافق قائمة السوق.


**لوحة إدارة كاملة بجلسة مستقلّة**

- كوكي `pa_admin` منفصل تمامًا عن `pa_session`، فيمكن العمل في لوحة الإدارة
  وحساب مستخدم عادي في المتصفّح نفسه معًا، والتنقّل بينهما بلا خروج من أحدهما.
  ومدّته 8 ساعات لا 14 يومًا لأن صلاحياته أوسع.
- حساب إداري `AdminAccount` منفصل عن `User` — لا حقل «دور» على المستخدم فلا
  يتسرّب إلى حمولة عامة ولا يصير رفع الصلاحية خطأً واردًا.
- تسع صفحات: المؤشرات · المستخدمون · ملفّ المستخدم · الإعلانات · الصفقات ·
  العرابين · الحركات المالية · الأسئلة الشائعة · الدخول.
- مؤشرات بثلاث مجموعات (النشاط، الأموال، الالتزامات) وقسم «يحتاج إجراءً الآن»
  يسرد الصفقات المتأخّرة مباشرة.
- سجلّ تدقيق لكل فعل إداري باسم منفّذه وحالته قبل وبعد.

**محفظة إلكترونية وعرابين**

- محفظة برصيدين: `balance` الكلي و`held` المحجوز، والمتاح فرقهما.
- كشف حساب مدين/دائن لكل مستخدم، قيوده لا تُعدَّل ولا تُحذف ويحمل كل قيد
  الرصيد بعده فيكون قابلًا للتدقيق سطرًا بسطر.
- **تقييد المزايدة بعربون محجوز**: من لا يكفي رصيده المتاح تُرفض مزايدته بـ
  `409 DEPOSIT_REQUIRED` قبل تسجيلها.
- العربون يُحجز مرّة واحدة لكل مزاد، ويُفكّ تلقائيًا للخاسرين فور انتهائه.
- إعدادات المزاد: العربون المطلوب، ومهلة سداد الفائز، وآليتا المصادرة والردّ.
- **مصادرة عربون المتخلّف عن السداد** — لا تُتاح إلا بعد انقضاء المهلة، وتعلّم
  الصفقة `defaulted` في العملية نفسها.
- إتمام الصفقة يخصم العربون المحجوز من قيمتها.
- صفحة `/account/wallet` بالرصيد والمحجوز والمتاح وكشف الحساب والعرابين.
- شحن وخصم إداريان موثّقان في كشف حساب المستخدم.

**الأسئلة الشائعة**

- إدارة كاملة من لوحة الإدارة: إضافة وتعديل وحذف وترتيب وتصنيف.
- صفحة `/faq` عامة مجمّعة بالتصنيف.
- علم `showOnListing` يُظهر السؤال أيضًا أسفل صفحة كل مزاد — لما يحتاجه
  المزايد قبل الضغط لا لكل الأسئلة.

**تجربة الجوال**

- **شريط مزايدة ثابت أسفل الشاشة** بحقل مبلغ وأزرار ضبط بالإبهام وعدّاد،
  لا ينزل المبلغ فيه عن آخر مزايدة أبدًا. كان زرّ المزايدة أسفل صفحة طويلة
  فيضطر المزايد للتمرير في كل مرة — وفي الثواني الأخيرة يكلّفه ذلك اللوحة.

### تغيّر

- **السعر الافتتاحي صار اختياريًا**: صفر يعني مزادًا مفتوحًا يبدأ من أول مزايدة.
- **شرح السعر الاحتياطي** في نموذج الإعلان: ما هو، ولماذا يبقى سرًّا، وماذا
  يحدث عند انتهاء الوقت.
- **تخطيط اللوحة يُقاس بالحبر لا بحجم الخط**: جدول مقاييس Arial ثابت
  (`arial-metrics.ts`) يوزّع الصفّين داخل شريطين بحدود محسوبة.
- حجم خطّ الحقول 16px على الجوال (14px على الشاشات الأكبر).
- احترام `prefers-reduced-motion` على مستوى المنصّة كلّها لا على رسمين فقط.
- `ORDER_STATUS_LABELS.awaiting_settlement` صار «بانتظار السداد» بدل
  «بانتظار الإتمام» بعد إضافة المهلة.
- **عرابين الخاسرين لم تعد تُفكّ عند رسوّ المزاد، بل بعد اكتمال سداد الفائز.**
  المزاد لا ينتهي بالرسوّ بل بالسداد: لو فُكّت عرابينهم لحظة الرسوّ ثم تخلّف
  الفائز، لتعذّرت إعادة الإرساء لأن مزايدات الباقين بلا مال خلفها. ويبقى
  الاستثناء: مزاد انتهى بلا بيع تُفكّ عرابينه كلّها فورًا.
- **المصادرة صارت بنسبة قابلة للضبط** (`forfeitPercent`) بدل «كل العربون
  دائمًا»، والباقي يعود إلى المتاح في العملية نفسها. وصفر يعطّلها تمامًا.
- **مهلة تراجع عن المصادرة** (`forfeitUndoWindowHours`): المصادرة قرار بشري
  يقع بضغطة وقد يقع على الاسم الخطأ. والتراجع لا يمحو شيئًا — قيد عكسي، ووسم
  `reversedAt` على قيد الإيراد، وعودة الصفقة إلى «بانتظار السداد».
- **حروف اللوحة ثلاث قوائم منسدلة** بدل حقل نصّ حرّ، كل قائمة تحمل الحرف العربي
  ومقابله اللاتيني — فلا يُدخَل ما ليس في جدول المرور أصلًا. والخانة لا تُفتح قبل
  ملء ما قبلها، وإفراغ خانة يُسقط ما بعدها.
- **الشكل المعتمد للحروف** صار ألفًا بلا همزة (`ا`) وياءً مقصورة (`ى`) مطابقةً
  لجدول لوحات المرور، والأشكال الأخرى تُطبَّع إليهما.
- **لوحة الدراجة النارية بلا شعار أوسط** وبارتفاع أقصر (`0 0 430 195`): الشعار
  كان يأكل عرضًا في لوحة ضيّقة فيصغر صفّاها، وحذفه وحّد ارتفاع اللوحات المعروضة
  في بطاقات السوق كلّها.
- **العدّاد في البطاقة صار صفًّا كامل العرض تحت اللوحة مباشرة** بوحداته
  المكتوبة، والسعر تحته — بدل شارة صغيرة في الزاوية.
- **الأخضر صار `#128a4e` داكنًا** (و`#006c35` أخضر العلم في السمة الفاتحة)
  أقرب إلى ألوان المنصّات الحكومية.
- **الاتجاه صار منطقيًا لا فيزيائيًا في كل المشروع**: `text-start`/`text-end`
  بدل `text-right`/`text-left`، فلا ينكسر شيء عند قلب سياق واحد إلى LTR.
- **رابط «تخطّي إلى المحتوى»** أوّل عنصر قابل للتركيز في كل صفحة — سوقًا
  وحسابًا وإدارةً — بدل المرور بالشعار وروابط التنقّل كلّها في كل مرة.
- **صفوف العرابين تحمل شروطها من الخادم** (`canForfeit` · `canRefund` ·
  `canUndo`) بدل أن تشتقّها الصفحة، فلا يظهر زرّ يرفضه الخادم ولا يُخفى إجراء
  مسموح.
- التذييل فقد فقرتَي التنويه القانوني: كانتا تكرّران على كل صفحة ما تشرحه
  «كيف يعمل» و«الأسئلة الشائعة» بتفصيل أوفى.

### أُصلح

- **أمان: مصادرة العربون كانت محروسة في الواجهة وحدها.** `forfeitDeposit` لم
  تكن تفحص إلا أن العربون `held`، والزرّ مخفيّ إن لم تنقضِ المهلة — فطلبٌ مباشر
  إلى `POST /api/admin/deposits/{id}` كان **يصادر عربونًا في مزاد ما زال جاريًا
  ولا صفقة له أصلًا**. صار الخادم يفحص وجود الصفقة وانقضاء مهلتها ونسبة
  المصادرة، ويرفض بـ`NO_ORDER_FOR_DEPOSIT` أو `FORFEIT_TOO_EARLY` أو
  `FORFEIT_DISABLED`. واختبار شامل يرسل الطلب متجاوزًا الواجهة فلا تعود الثغرة
  صامتة.
- **ردّ العربون كان بلا حارس إطلاقًا**: كان يمكن فكّ عربون مزايد هو الأعلى في
  مزاد شغّال، فتبقى مزايدته قائمة بلا مال يضمنها. صار يُرفض بـ
  `BIDS_STILL_STANDING`.
- **إغلاق البائع لصفقته كان يترك العرابين محجوزة إلى الأبد**: الآثار المالية
  للاكتمال كانت مكتوبة في مسار الإدارة وحده، ومسار البائع يغيّر الحالة فقط.
  وُحِّد المساران في `applyOrderCompletion`.
- **`forfeitDepositOnDefault` علم ميت**: كان محفوظًا في كل إعلان ومثبّتًا على
  `true` ولا يقرؤه سطر واحد، فيوحي بمفتاح حوكمة غير موجود. حلّ محلّه
  `forfeitPercent` الفعلي.
- **البحث برقم مرجعي كان يطابق نصًّا**: «#1» في جدول فيه عشرون إعلانًا كان
  يسرد 1 و10 و11 حتى 19. صار يُحلَّل الرقم ويُقارَن شكله المعياري.
- **أعمدة جداول الإدارة تنزاح عن عناوينها**: كان العنوان يحمل محاذاته والخلية
  محاذاتها، وكان العنوان `text-right` **فيزيائيًا** بينما الخلية منطقية. صار
  العمود يُعلَن مرّة واحدة في `columns` فتُبنى منه الترويسة والمحاذاة والعرض،
  وقاعدة CSS واحدة تحكم العنوان والخلية معًا.
- **لا معلَم `main` في لوحة الإدارة**: كان محتواها كلّه خارج أي landmark يقفز
  إليه قارئ الشاشة.
- **الحُزم ترجع 404 فيظهر الموقع بلا تنسيق ولا تفاعل**: `pnpm test:e2e` كان يبني
  مبنى إنتاج داخل `.next` نفسه الذي يخدمه خادم التطوير العامل، فيستبدل ملفاته
  ويصير الخادم يشير إلى حُزم لم تعد موجودة. صار مبنى الاختبار الشامل يكتب في
  `.next-e2e` عبر `distDir` القابل للضبط من البيئة، فلا يمسّ خادمًا عاملًا.
- **صفحة 404 تذكر «معرّف الغرفة»** — نصّ متبقٍّ من مفهوم الغرف المحذوف. استُبدل
  بنصّ يخصّ اللوحات، وأُضيف زرّ «تصفّح السوق».
- **صفحتا 404 والخطأ بخلفية داكنة** بينما بقيّة المنصّة فاتحة: كانتا الصفحتين
  الوحيدتين خارج `PageShell`. غُلِّفتا به.
- **`themeColor` داكن** في بيانات الصفحة بينما الواجهة فاتحة — صار يطابق خلفية
  السمة الفاتحة.
- **مربّع اختيار الشعار أسود في السمة الفاتحة**: كان `bg-paper`، و`--color-paper`
  هو لون **النص** فينقلب أسود، فيختفي الشعار الأسود داخله. صار أبيض ثابتًا لأنه
  يحاكي وجه اللوحة لا لون النص.
- **إبهام مفتاح التبديل أسود** على مسار ذهبي داكن للسبب نفسه — صار أبيض ثابتًا.
- **حروف اللوحة وأرقامها تلامس الإطار وتخرج منه**: حرف «أ» ارتفاعه 0.876em
  بينما الحرف اللاتيني 0.728em، ومع معامل تعويض ثابت كان يتجاوز الصفّ ويخرج من
  اللوحة فعلًا. القياس أثبت أن الأرقام العربية تبعد 2.5px فقط عن الحافة العليا
  مقابل 15px سفلًا. الآن الهامش 17–18px أعلى وأسفل في كل اللوحات.
- **«السعودية» الرأسية تُقصّ عند حافة اللوحة**: النصّ يدور 90° فيصير نصف
  ارتفاعه امتدادًا أفقيًا، وكان يتجاوز الإطار بـ 3.4px على لوحة الدراجة.
- **جلسة موقّعة لمستخدم محذوف تُنتج صفحة حساب فارغة**: الترويسة تقول «دخول»
  والاسم فارغ وكل الأرقام صفر. الحرّاس صاروا يتحقّقون من **وجود الحساب** لا من
  صحّة توقيع الجلسة وحدها — في الصفحات وفي `requireUserId` معًا.
- **مربّع اختيار الشعار ومفتاح التبديل أسودان** في السمة الفاتحة: كلاهما كان
  `bg-paper`، و`--color-paper` هو لون النص فينقلب أسود.
- **تجاوز أفقي في لوحة الإدارة**: `overflow-x-auto` كان على `ul` لا على `nav`،
  وعنصر الشبكة عرضه تلقائي فيتمدّد بعرض محتواه ما لم يكن هو حاوية التمرير.
- **خلفية `html` داكنة خلف محتوى فاتح**: ما يظهر خارج صندوق `body` يُرسم
  بخلفية `html`، فكانت تظهر رقعة داكنة عند التمرير الأفقي أو ارتداده.
- **تكبير iOS التلقائي عند التركيز على حقل**: سفاري يُكبّر الصفحة لأي حقل خطّه
  أصغر من 16px.
- **تضارب أنواع المسارات بين مجلّدَي البناء**: لكل مجلّد الآن ملفّ `tsconfig`
  خاص به عبر `typescript.tsconfigPath`.
- **39 عنصرًا تُرسَل من الخادم بشفافية صفر**: حركات الدخول المبنيّة على
  جافاسكربت تُصيَّر مخفية، فلو تعطّل السكربت أو تأخّر بقيت الصفحة الرئيسية
  وبطاقات السوق **غير مرئية نهائيًا**. نُقلت حركات الدخول كلّها إلى CSS،
  و`HomeHero` صار مكوّن خادم بلا جافاسكربت. يحرسه اختبار شامل الآن.
- **ارتفاعات البطاقات متفاوتة** في الصفّ الواحد: لوحة الدراجة نسبتها 1.43:1
  والطويلة 4.65:1. أُضيف حجم `fill` وصندوق بنسبة ثابتة فتساوت.
- **عدّاد بطاقات السوق جامد**: كان يعرض قيمة `remainingMs` القادمة مع الجلب
  ولا يتحرّك حتى مزامنة تالية. صار وقت الخادم يرافق قائمة السوق فيتحرّك حيًّا.
- **صيغة العدّاد المضغوط ملتبسة**: «2 يومان 23:58» تُقرأ ساعةً:دقيقة بلا دلالة.
  صارت «2 يومان و23 س»، وتظهر الثواني لما دون اليوم حيث تُحسب فعلًا.
- **الأختام الزمنية بلا تاريخ**: كشف المزايدات والعروض والطلبات كانت تعرض الوقت
  وحده (`07:02`) — بقيّة من عصر البثّ حيث كل شيء يقع خلال دقائق. الآن المزاد
  يمتدّ حتى 30 يومًا، فلا يُعرف يوم المزايدة. `formatTimestamp` الجديدة تعرض
  الوقت لأحداث اليوم، و«أمس» للأمس، والتاريخ لما هو أقدم، والسنة لما تجاوز
  السنة الحالية.
- **التواريخ تتبع منطقة الجهاز الزمنية**: كل التنسيق مثبّت الآن على
  `Asia/Riyadh`، فيرى البائع والمشتري المرجع الزمني نفسه، ويتطابق ناتج الخادم
  مع ناتج العميل فلا يقع اختلاف في الترطيب.

## [1.0.0] — 2026-08-31

أول إصدار مستقرّ للمنصّة بوصفها **سوق ويب لتداول اللوحات**. هذا الإصدار يتضمّن
تحوّلًا جذريًا عن أصل المشروع (أداة مزادات أثناء بثّ تيك توك)، ولذلك يحمل
تغييرات كاسرة واسعة.

### أُضيف

**التحديثات اللحظية عبر WebSocket**

- خادم مخصّص [`server.mjs`](server.mjs) يستضيف Next وخادم `ws` في العملية
  نفسها، ويوجّه ترقيات `/ws` إلى WebSocket وما عداها إلى Next.
- بروتوكول اشتراك بالمواضيع: `listing:{id}` للإعلان الواحد، و`market` لشبكة
  السوق. اتصال واحد يخدم عدة مواضيع (حدّ 20 موضوعًا لكل مقبس).
- **رقم تسلسلي لكل موضوع** يمكّن العميل من كشف أي حدث مفقود وطلب مزامنة فورية.
- مصافحة `welcome` تسلّم آخر تسلسل لكل موضوع عند الاشتراك.
- **حضور المشاهدين**: حدث `presence` يبثّ عدد المتصلين بكل إعلان.
- نبضة `ping`/`pong` من الطرفين تكشف الاتصالات الميتة (30 ثانية من الخادم،
  25 من العميل، وحدّ صمت 45 ثانية).
- صمود الاتصال في العميل: مزامنة كاملة عند كل اتصال وعند عودة التبويب أو
  الشبكة، تراجع أُسّي مع نثر، وسقوط احتياطي إلى جلب دوري كل 8 ثوانٍ بعد ثلاث
  محاولات فاشلة.
- تجميع المزامنة في نافذة 200 مللي — عدة أحداث متتابعة تنتج طلبًا واحدًا.
- `POST /api/internal/sweep` محمي بمفتاح الجلسة، يستدعيه الخادم كل 5 ثوانٍ،
  فينتهي المزاد في وقته حتى لو لم يفتح أحد أي صفحة.
- 5 اختبارات وحدة للطبقة اللحظية: التوجيه بالموضوع، التسلسل المستقل لكل
  موضوع، مقاومة المقبس المعطوب، وعدّ المشاهدين.

**السوق والتداول**

- ثلاث طرق للعرض: **مزاد** بمزايدات وعدّاد وتمديد تلقائي وسعر احتياطي مخفي،
  **بيع مباشر** بضغطة واحدة، و**استقبال عروض** يختار البائع منها.
- كيانات `Offer` و`Order` بآلات حالة كاملة. قبول العرض يُغلق الإعلان وينشئ
  طلبًا ويرفض بقية العروض المعلّقة.
- شراء مباشر ذرّي — لا تُباع اللوحة لمشتريين متزامنين.
- بحث يقبل الحروف عربية أو لاتينية والأرقام عربية أو غربية، مع فلاتر (طريقة
  البيع، نوع اللوحة، التوفّر) وخمسة أوجه فرز.

**صفحات الحساب**

- لوحاتي · مزايداتي · العروض (مرسلة وواردة) · مشترياتي · مبيعاتي · الإعدادات.
- إضافة لوحة بمعاينة حيّة، ونشر وتعديل وإلغاء وإعادة عرض.
- البائع يرى `reserveGap` — الفارق المتبقي حتى بلوغ الاحتياطي.

**التوثيق**

- توثيق مرجعي كامل: البنية المعمارية، نموذج المجال، مرجع الواجهة البرمجية،
  بروتوكول اللحظية، تصميم اللوحة، الأمان، استراتيجية الاختبار.
- ثمانية قرارات معمارية موثّقة في [`docs/adr/`](docs/adr/).
- دليل مساهمة، وسجلّ التغييرات هذا.

### تغيّر

- **التشغيل**: `pnpm dev` و`pnpm start` يشغّلان `node server.mjs` بدل
  `next dev` و`next start`. النشر يحتاج بيئة Node طويلة العمر.
- **الأدوار**: لا «مضيف» ولا «مزايد» — كل مستخدم بائع ومشترٍ بحساب واحد.
- **الإعلان = لوحة واحدة**. حُذف التجميع نهائيًا، حتى في لوحة التحكّم.
- **بنية معلومات مسطّحة**: `/market` يعرض كل الإعلانات مباشرة، و`/market/{id}`
  هي صفحة الإعلان الوحيدة.
- **مدّة المزاد** من 5 دقائق إلى 30 يومًا بدل دقائق البثّ.
- **خلفية السوق فاتحة** عبر تجاوز الرموز بـ `data-theme="light"`، مع بقاء
  منظومة الرموز الواحدة.
- **خط اللوحة Arial** للصفّين العربي واللاتيني كاللوحة الحقيقية، مع معاملات
  تعويض لمقاييس Arial (الأرقام العربية 1.3، الحروف 1.15) وإلا ظهر الصف العربي
  أصغر بوضوح.
- **تصميم اللوحة** أعيد ليطابق الصورة المرجعية: وجه أبيض مسطّح، إطار أسود
  رفيع، بلا فواصل داخلية، ونسبة ≈ 4.65:1 للوحة الطويلة.
- **الشعار الافتراضي** صار `public/plate-emblems/palm.jpeg` مُلوَّنًا عبر قناع
  SVG بدل مسار متجه.

### حُذف

كل ما يخصّ بثّ تيك توك، بعد أن قيّد تيك توك هذا الاستخدام
([ADR-0008](docs/adr/0008-pivot-to-marketplace.md)):

- صفحة البثّ `/live/{roomSlug}` وأوضاع العرض الثلاثة وشفافية OBS.
- الغرف بالكامل: `/rooms`، ولوحة تحكّم المضيف، ومسارات `/api/host/rooms/*`.
- صفحة المزايدة المنفصلة `/bid/{slug}` — الصفحة الموحّدة متجاوبة وتغني عنها.
- طابور اللوحات و«اللوحة النشطة» وترتيب العرض.
- نقل SSE و`event-bus.ts` — استبدلهما WebSocket.

### أُصلح

- **`instanceof` يفشل عبر حدود الحزم**: أخطاء المجال كانت تفقد `status`
  و`code` فتعود `400` عامة بدل `409` برمزها. استُبدل بعلامات بنيوية
  (`isServiceError` / `isTradeError`) — [ADR-0004](docs/adr/0004-structural-error-markers.md).
- **معرّفات SVG غير مستقرّة**: عدّاد الوحدة أعطى معرّفات مختلفة على الخادم
  والعميل، فنتج عدم تطابق في الترطيب **و**قناع يشير إلى شعار خاطئ (ظهر كصندوق
  ذهبي). استُبدل بتجزئة مشتقّة من المحتوى — [ADR-0007](docs/adr/0007-stable-svg-ids.md).
- **العدّاد يعرض `0` أثناء التصيير على الخادم**: صار يُهيّأ من `endsAt`
  و`serverTime` القادمين من الخادم.
- **`formatCountdown` يعرض «4318:59»** للمزادات متعدّدة الأيام: أُعيدت كتابته
  بأيام وساعات ودقائق وثوانٍ مع تصريف عربي صحيح، وأُضيفت له اختبارات.
- **نموذج الدخول لا يُرسَل بصمت** بسبب حقل مخفي يفشل في التحقّق: نُقل `mode`
  إلى قيم النموذج مع `superRefine`، وأُضيف تنبيه احتياطي عند فشل التحقّق.
- **`<a>` متداخل** داخل بطاقة — عطل ترطيب حقيقي: نُقل التذييل خارج رابط البطاقة.
- **`getUpgradeHandler()` قبل `prepare()`** يرمي عند الإقلاع: نُقل الاستدعاء
  بعد `await app.prepare()`.
- **مسار محذوف ما زال يُستدعى** من مكوّن — كشفه الاختبار الشامل وحده.

### أمان

- السعر الاحتياطي **لا يغادر الخادم إطلاقًا**؛ الزائر يرى حالة مشتقّة من ثلاث
  قيم. يحرسه اختبار يقارن مفاتيح الحمولة بقائمة بيضاء صريحة، فأي حقل جديد
  يسرّبه يُسقِط الاختبار.
- أسماء المزايدين مقنّعة في الكشف العام، ولا تظهر أرقام الجوال في أي حمولة عامة.
- `SESSION_SECRET` إلزامي في الإنتاج — يرمي عند أول عملية توقيع أو تحقّق إن
  كان مفقودًا أو أقصر من 16 حرفًا.
- كلمات المرور بـ `scrypt` وملح لكل مستخدم، والمقارنات ثابتة الزمن
  (`timingSafeEqual`) في التحقّق من كلمة المرور وتوقيع الجلسة.
- رسالة واحدة لخطأ البريد ولخطأ كلمة المرور، فلا يُكشف وجود الحساب.
- تحديد معدل على التداول (6 لكل 10 ثوانٍ لكل مستخدم لكل إعلان) وعلى الدخول
  وإنشاء الحساب.
- `robots.txt` يمنع فهرسة `/account` و`/api` و`/login` و`/register`.
- `handleError` لا يسرّب أثرًا مكدّسًا ولا تفاصيل تنفيذ، ولا تُسجَّل أسرار في
  أي مخرج.

---

## [0.2.0] — 2026-08-30

تحسينات تجربة المستخدم على منصّة المزادات، قبل التحوّل إلى السوق.

### أُضيف

- صفحة مزادات عامة بفلاتر وفرز وعرض بطاقات.
- صفحة تفاصيل لكل مزاد مع كشف المزايدات الكامل.
- تنسيق عدّاد يدعم المدد الطويلة بأيام وساعات.
- خريطة موقع و`robots.txt`.

### تغيّر

- تسطيح `/auctions` لتعرض كل المزادات مباشرة بدل تجميعها في مجموعات.
- إعادة تصميم شاشة البثّ لتحتوي كامل المحتوى داخل حدود الشاشة (كانت تظهر حافة
  اللوحة وحدها) — بتقييد الأبعاد طولًا وعرضًا معًا.

---

## [0.1.0] — 2026-08-30

الإصدار الأول: أداة لإدارة مزادات اللوحات **أثناء بثّ تيك توك المباشر**.

### أُضيف

- غرف بثّ برابط ثابت `/live/{roomSlug}` يُضاف مرّة واحدة في TikTok LIVE Studio
  أو OBS، بثلاثة أوضاع عرض وشفافية حقيقية.
- لوحة تحكّم للمضيف وصفحة مزايدة عامة.
- مكوّن `SaudiLicensePlate` بثلاثة أنواع لوحات وخريطة حروف مخصّصة للوحات
  السعودية، وشعارات SVG أصلية.
- محرّك المزاد: مزايدة ذرّية على الخادم، تمديد تلقائي، سعر احتياطي مخفي،
  ونتيجة محسوبة.
- المال بأعداد صحيحة من الهللات، وجلسات موقّعة، وتحديد معدل، وتخزين مجرّد خلف
  واجهة واحدة.

---

[1.0.0]: #100--2026-08-31
[0.2.0]: #020--2026-08-30
[0.1.0]: #010--2026-08-30
