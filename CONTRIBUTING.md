# دليل المساهمة

> اقرأ [البنية المعمارية](docs/architecture.md) أولًا. هذا الملف يشرح **كيف
> نكتب** لا **ماذا بُني**.

## البدء

```bash
pnpm install
cp .env.example .env.local
pnpm dev
```

## بوابة ما قبل الدفع

كلها يجب أن تمرّ:

```bash
pnpm typecheck && pnpm lint && pnpm test && pnpm test:e2e
```

`pnpm test:e2e` يبني مبنى إنتاج في `.next-e2e` لا في `.next`، فيمكن تشغيله
وخادم التطوير يعمل. قبل هذا العزل كان البناء يستبدل ملفات `.next` تحت الخادم
العامل، فترجع الحُزم 404 ويظهر الموقع بلا تنسيق ولا تفاعل — إن رأيت ذلك يومًا،
فالعلاج `rm -rf .next` وإعادة تشغيل `pnpm dev`.

## قواعد الطبقات

الاتجاه أحادي، والأدنى لا يعرف الأعلى:

```
app / components  →  lib/server  →  lib/domain
                                 →  lib/store
```

| الطبقة | يُسمح | يُمنع |
| --- | --- | --- |
| `lib/domain` | دوال نقية، أنواع | أي I/O، `Date.now()` داخليًا، React، استيراد من `server` أو `store` |
| `lib/store` | تخزين، ذرّية، منع تكرار | قواعد عمل |
| `lib/server` | تنسيق، جلسات، نشر لحظي | JSX |
| `app/api` | تحقّق Zod، مصادقة، تحويل أخطاء | منطق عمل |
| `components` | عرض | وصول مباشر للتخزين |

**المجال لا يقرأ الوقت بنفسه**: يُمرَّر `nowMs` وسيطًا. بهذا تُختبر النهايات
والتمديد بلا انتظار ولا ساعات وهمية.

## أين تضع الكود الجديد

| نوع التغيير | المكان |
| --- | --- |
| قاعدة عمل جديدة | `lib/domain/auction.ts` + اختبار وحدة |
| تنسيق بين عدة كيانات | `lib/server/market-service.ts` |
| مسار HTTP جديد | `app/api/**/route.ts` — تحقّق فقط، والمنطق في الخدمة |
| عملية تخزين | أضفها إلى واجهة `AuctionStore` **ثم** نفّذها |
| حدث لحظي جديد | `ListingEventType` + نشر داخل `publish()` في الخدمة |

## قواعد لا تُخالَف

1. **كل قاعدة حرجة على الخادم.** ما في العميل تحسين تجربة لا حراسة، ويُعاد
   التحقّق منه دائمًا.
2. **لا أسرار في الكود** ولا في السجلّات. متغيّرات بيئة فقط.
3. **السعر الاحتياطي لا يغادر الخادم.** أي حقل جديد في `ListingDetail` يمرّ
   على اختبار القائمة البيضاء.
4. **لا أرقام جوال ولا بريد** في أي حمولة عامة، والأسماء مقنّعة في الكشف.
5. **المال أعداد صحيحة من الهللات.** التحويل عند الحدود فقط.
6. **لا `instanceof` لأخطاء المجال** — استخدم الحارس البنيوي
   ([ADR-0004](docs/adr/0004-structural-error-markers.md)).
7. **لا تُوثق بقيمة من العميل.** المبلغ المطلوب والوقت والهوية تُشتق على الخادم.
8. **معرّفات SVG من المحتوى** لا من عدّاد ولا عشوائية
   ([ADR-0007](docs/adr/0007-stable-svg-ids.md)).

## أسلوب الكتابة

**التعليقات بالعربية، والمعرّفات بالإنجليزية.** هذا أسلوب المشروع كلّه.

التعليق يشرح **لماذا** لا **ماذا**:

```ts
// ✗ يزيد التسلسل بواحد
// ✓ التسلسل لكل موضوع على حدة، فيكتشف العميل الحدث المفقود ويطلب مزامنة كاملة
```

التعليقات المفيدة في هذا المشروع تشرح عادة: قيدًا خارجيًا (مقاييس Arial)، أو
فخًّا وقعنا فيه (`prepare()` قبل `getUpgradeHandler()`)، أو سبب رفض بديل أوضح.

**TypeScript**: `strict` مفعّل. تجنّب `any`؛ استخدم `unknown` مع تضييق. صدّر
الأنواع مع الدوال التي تستعملها.

**التسمية**: `assertX` للتحقّق الذي يرمي · `computeX` للحساب النقي ·
`getX` للقراءة · `isX` للحارس أو المسند.

**الألوان**: `--color-paper` هو لون **النص** لا سطح أبيض — وينقلب أسود في السمة
الفاتحة. لسطح أبيض ثابت (وجه لوحة، إبهام مفتاح) استخدم `bg-white` صراحةً.

**التواريخ**: استخدم `formatTimestamp` لا `Intl` مباشرة. التنسيق مثبّت على
`Asia/Riyadh` ليكون المرجع واحدًا ولئلّا يختلف ناتج الخادم عن العميل.

**الواجهة**: RTL أولًا؛ كل نصّ ظاهر بالعربية؛ والتجاوب مطلوب — اختبار شامل
يفشل عند وجود تمرير أفقي في عرض 380px.

## الاختبارات

كل قاعدة عمل تحتاج اختبار وحدة. الاختبار الشامل للرحلات لا للحالات الحدّية.

```ts
beforeEach(() => {
  db = emptyDatabase()
  seedDatabase(db)
  store = new MemoryStore(db)
  resetStoreForTests(store)   // الخدمات تقرأ المخزن من globalThis
  resetRateLimits()           // وإلا سرّبت حالة العدّاد بين الاختبارات
})
```

أسماء `describe`/`it` بالعربية — المخرجات تُقرأ كوصف للسلوك.

**في Playwright**: `await expect(locator).toBeVisible()` ينتظر، أمّا
`isVisible()` و`count()` فلا. للنصّ المتكرّر استخدم `.first()` أو محدّدًا أدقّ.
تفاصيل أكثر — ودروس من أخطاء وقعت فعلًا — في [استراتيجية الاختبار](docs/testing.md).

## القرارات المعمارية

قرار يغيّر بنية أو يرفض بديلًا معقولًا ⇐ اكتب ADR جديدًا في `docs/adr/`
بالترقيم التالي، وأضفه إلى [الفهرس](docs/adr/README.md). الصيغة:
سياق ← قرار ← بدائل مرفوضة ← نتائج ← متى يُعاد النظر.

اذكر **البدائل المرفوضة ولماذا** — هذا أنفع جزء بعد سنة.

## سجلّ التغييرات

كل تغيير ملحوظ يُسجَّل في [`CHANGELOG.md`](CHANGELOG.md) تحت `[غير منشور]`
بتصنيفه (أُضيف · تغيّر · حُذف · أُصلح · أمان). للإصلاحات اذكر **العطل** لا
التعديل فقط — سجلّ الأعطال أنفع من سجلّ الملفات.

## هيكل الالتزام

```
feat(market): إضافة فلتر التوفّر إلى شبكة السوق
fix(realtime): مزامنة فورية عند اكتشاف فجوة في التسلسل
docs(adr): توثيق فصل الأوامر عن الأحداث
```

الأنواع: `feat` · `fix` · `docs` · `refactor` · `test` · `chore`.
النطاقات الشائعة: `market` · `realtime` · `plate` · `account` · `auth` · `store`.
